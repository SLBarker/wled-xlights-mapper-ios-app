---
phase: 02-hero-carousel
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03

must_haves:
  truths:
    - "Hero section shows exactly one phone mockup at a time — no fan strip"
    - "Prev and next arrow buttons flank the phone and advance the carousel"
    - "Dot indicators below the phone reflect the current slide (active dot uses --accent)"
    - "Carousel loops from last slide back to first"
    - "Auto-advance fires every 4 seconds and pauses on mouseenter"
    - "Left/right swipe on mobile advances the carousel"
    - "Adding a new slide requires only adding one .hero-carousel__slide div to the HTML"
    - "All four slides present: Connect, Scan, Preview & Export, Results"
  artifacts:
    - path: "index.html"
      provides: "Carousel HTML markup (4 slides) + carousel JS"
      contains: "hero-carousel__track"
    - path: "resources/screenshot/connect.jpeg"
      provides: "Connect slide image"
    - path: "resources/screenshot/ar.jpeg"
      provides: "Scan slide image"
    - path: "resources/screenshot/preview.jpeg"
      provides: "Preview & Export slide image"
    - path: "resources/screenshot/results.jpeg"
      provides: "Results slide image"
  key_links:
    - from: "prev/next button click"
      to: "track translateX"
      via: "JS goTo() sets style.transform"
      pattern: "addEventListener.*click.*goTo"
    - from: "touchstart / touchend"
      to: "goTo()"
      via: "swipe delta > 40px triggers advance"
      pattern: "addEventListener.*touchstart"
    - from: "setInterval"
      to: "goTo()"
      via: "auto-advance every 4000ms"
      pattern: "setInterval"
    - from: ".hero-carousel__dot"
      to: ".hero-carousel__dot--active"
      via: "JS toggles class on current index"
      pattern: "hero-carousel__dot--active"
---

<objective>
Replace the `.hero__phones` HTML markup with the 4-slide carousel structure, then add a self-contained carousel JavaScript IIFE to the existing `<script>` area at the bottom of `<body>`.

Purpose: Deliver the full working carousel — markup + behaviour — so visitors see one phone at a time, can advance with arrows, dots, and swipe, and the carousel auto-advances on a 4-second loop.

Output: `index.html` with carousel HTML and carousel JS complete. The visual result is a fully functional sliding carousel with all four app screens.
</objective>

<execution_context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/phases/02-hero-carousel/02-CONTEXT.md
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/PROJECT.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/ROADMAP.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/codebase/CONVENTIONS.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/phases/02-hero-carousel/02-01-SUMMARY.md

<interfaces>
<!-- BEM classes defined in Plan 01 CSS — use these exact names -->
.hero-carousel          (flex-column container, margin-top: 6rem, fadeUp animation)
.hero-carousel__viewport  (flex-row: [arrow] [track-wrap] [arrow])
.hero-carousel__track-wrap  (overflow:hidden, width:240px)
.hero-carousel__track   (flex-row of slides, CSS transform: translateX drives animation)
.hero-carousel__slide   (flex:0 0 240px, flex-column, align-items:center)
.hero-carousel__arrow   (circular 44px button, --surface bg, --accent colour)
.hero-carousel__dots    (flex list, margin-top:1.25rem)
.hero-carousel__dot     (8px circle button, rgba(0,0,0,0.2) bg)
.hero-carousel__dot--active  (bg: var(--accent), scale(1.25))

<!-- .phone-bezel and children — reused unchanged inside each slide -->
.phone-bezel / .phone-bezel__frame / .phone-bezel__screen / .phone-bezel__notch / .phone-bezel__caption

<!-- Existing script blocks (end of body) — append carousel IIFE BEFORE closing </body> -->
<!-- Block 1: IntersectionObserver scroll-reveal (const animatedEls ...) -->
<!-- Block 2: Hamburger nav toggle IIFE -->
<!-- Block 3: Lazy video loading IIFE -->
<!-- Block 4 (NEW): Hero carousel IIFE -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace .hero__phones HTML with 4-slide carousel markup</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 954–1005 (current hero section markup — the .hero__phones div to replace)
    - index.html lines 246–260 (confirm .hero__phones CSS is gone after Plan 01)
    - resources/screenshot/ (confirm all four JPEG filenames exist: connect.jpeg, ar.jpeg, preview.jpeg, results.jpeg)
  </read_first>
  <action>
Replace the entire `<!-- Hero phone mockups -->` comment block and the `.hero__phones` div (lines 972–1003 in the original file — locate by searching for `class="hero__phones"`) with the following carousel markup. The replacement goes in the same position, still inside the `<div class="container">` of the hero section.

**Replacement markup (exact):**

```html
        <!-- Hero carousel -->
        <div class="hero-carousel" role="region" aria-label="App screenshot carousel" aria-roledescription="carousel">

          <div class="hero-carousel__viewport">

            <!-- Prev arrow -->
            <button class="hero-carousel__arrow" id="carousel-prev" aria-label="Previous slide">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <polyline points="15 18 9 12 15 6"></polyline>
              </svg>
            </button>

            <!-- Slides -->
            <div class="hero-carousel__track-wrap">
              <div class="hero-carousel__track" id="carousel-track">

                <div class="hero-carousel__slide" role="group" aria-roledescription="slide" aria-label="1 of 4: Connect">
                  <div class="phone-bezel">
                    <div class="phone-bezel__frame">
                      <div class="phone-bezel__screen">
                        <div class="phone-bezel__notch" aria-hidden="true"></div>
                        <img src="resources/screenshot/connect.jpeg" alt="App screenshot showing the WLED controller connect screen with mDNS discovery list">
                      </div>
                    </div>
                    <p class="phone-bezel__caption">Connect</p>
                  </div>
                </div>

                <div class="hero-carousel__slide" role="group" aria-roledescription="slide" aria-label="2 of 4: Scan">
                  <div class="phone-bezel">
                    <div class="phone-bezel__frame">
                      <div class="phone-bezel__screen">
                        <div class="phone-bezel__notch" aria-hidden="true"></div>
                        <img src="resources/screenshot/ar.jpeg" alt="App screenshot showing the AR scanning view with gray-code pattern detection overlay">
                      </div>
                    </div>
                    <p class="phone-bezel__caption">Scan</p>
                  </div>
                </div>

                <div class="hero-carousel__slide" role="group" aria-roledescription="slide" aria-label="3 of 4: Preview &amp; Export">
                  <div class="phone-bezel">
                    <div class="phone-bezel__frame">
                      <div class="phone-bezel__screen">
                        <div class="phone-bezel__notch" aria-hidden="true"></div>
                        <img src="resources/screenshot/preview.jpeg" alt="App screenshot showing the 3D LED preview with quality tier colour overlay">
                      </div>
                    </div>
                    <p class="phone-bezel__caption">Preview &amp; Export</p>
                  </div>
                </div>

                <div class="hero-carousel__slide" role="group" aria-roledescription="slide" aria-label="4 of 4: Results">
                  <div class="phone-bezel">
                    <div class="phone-bezel__frame">
                      <div class="phone-bezel__screen">
                        <div class="phone-bezel__notch" aria-hidden="true"></div>
                        <img src="resources/screenshot/results.jpeg" alt="App screenshot showing the export results screen with LED position data">
                      </div>
                    </div>
                    <p class="phone-bezel__caption">Results</p>
                  </div>
                </div>

              </div><!-- /.hero-carousel__track -->
            </div><!-- /.hero-carousel__track-wrap -->

            <!-- Next arrow -->
            <button class="hero-carousel__arrow" id="carousel-next" aria-label="Next slide">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <polyline points="9 18 15 12 9 6"></polyline>
              </svg>
            </button>

          </div><!-- /.hero-carousel__viewport -->

          <!-- Dot indicators -->
          <ol class="hero-carousel__dots" id="carousel-dots" aria-label="Carousel slide indicators">
            <li><button class="hero-carousel__dot hero-carousel__dot--active" aria-label="Go to slide 1" aria-current="true"></button></li>
            <li><button class="hero-carousel__dot" aria-label="Go to slide 2"></button></li>
            <li><button class="hero-carousel__dot" aria-label="Go to slide 3"></button></li>
            <li><button class="hero-carousel__dot" aria-label="Go to slide 4"></button></li>
          </ol>

        </div><!-- /.hero-carousel -->
```

Notes:
- The `results.jpeg` slide is new — it was absent from the original markup (the original had only 3 slides: connect, ar, preview).
- Do NOT retain `role="img"` on the carousel container; use `role="region"` with `aria-roledescription="carousel"` as above.
- The caption "Preview &amp; Export" uses HTML entity for the ampersand (per D-02).
  </action>
  <verify>
    <automated>
      # Old hero__phones div gone
      grep -c 'class="hero__phones"' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # All 4 slides present
      grep -c 'hero-carousel__slide' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # All 4 screenshot images referenced
      grep -c 'resources/screenshot/connect.jpeg' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html
      grep -c 'resources/screenshot/results.jpeg' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Arrows present
      grep -c 'carousel-prev' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html
      grep -c 'carousel-next' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Dots list present
      grep -c 'carousel-dots' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Active dot on first slide
      grep -c 'hero-carousel__dot--active' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="hero__phones"' index.html` returns 0
    - `grep -c 'hero-carousel__slide' index.html` returns exactly 4 (one opening div per slide)
    - `grep -c 'resources/screenshot/connect.jpeg' index.html` returns 1
    - `grep -c 'resources/screenshot/ar.jpeg' index.html` returns at least 1
    - `grep -c 'resources/screenshot/preview.jpeg' index.html` returns at least 1
    - `grep -c 'resources/screenshot/results.jpeg' index.html` returns 1 (results.jpeg is new to the hero)
    - `grep -c 'id="carousel-prev"' index.html` returns 1
    - `grep -c 'id="carousel-next"' index.html` returns 1
    - `grep -c 'id="carousel-dots"' index.html` returns 1
    - `grep -c 'hero-carousel__dot--active' index.html` returns at least 1 (initial active dot)
    - `grep -c 'role="region"' index.html` returns at least 1 (carousel region role)
    - `grep -c 'aria-roledescription="carousel"' index.html` returns 1
  </acceptance_criteria>
  <done>
    The hero section contains `.hero-carousel` with 4 slides (connect, ar, preview, results), prev/next arrow buttons with inline SVG chevrons, and a dot indicator list. No `.hero__phones` div remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add carousel JavaScript IIFE</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 1430–1532 (existing script blocks — append after the lazy video IIFE, before </body>)
  </read_first>
  <action>
Add a new `<script>` block immediately before the closing `</body>` tag (after the existing lazy video IIFE). The script is a self-contained IIFE. Use `function` keyword and `var` declarations only where needed for older compatibility — but per project convention use `const` and arrow functions (consistent with the IntersectionObserver block).

**Full script block to insert (exact):**

```html
  <script>
    /* ── Hero carousel ── */
    (function () {
      const track     = document.getElementById('carousel-track');
      const prevBtn   = document.getElementById('carousel-prev');
      const nextBtn   = document.getElementById('carousel-next');
      const dotsWrap  = document.getElementById('carousel-dots');

      if (!track || !prevBtn || !nextBtn || !dotsWrap) return;

      const slides    = Array.from(track.querySelectorAll('.hero-carousel__slide'));
      const dots      = Array.from(dotsWrap.querySelectorAll('.hero-carousel__dot'));
      const total     = slides.length;
      let   current   = 0;
      let   timer     = null;

      // Move carousel to slide at index n (loops)
      function goTo(n) {
        current = ((n % total) + total) % total;
        track.style.transform = `translateX(-${current * 100}%)`;
        dots.forEach((dot, i) => {
          const active = i === current;
          dot.classList.toggle('hero-carousel__dot--active', active);
          dot.setAttribute('aria-current', active ? 'true' : 'false');
        });
        slides.forEach((slide, i) => {
          slide.setAttribute('aria-hidden', i === current ? 'false' : 'true');
        });
      }

      // Initialise: hide non-active slides from AT
      goTo(0);

      // Auto-advance
      function startTimer() {
        timer = setInterval(() => goTo(current + 1), 4000);
      }
      function stopTimer() {
        clearInterval(timer);
        timer = null;
      }
      startTimer();

      // Arrow buttons
      prevBtn.addEventListener('click', () => { stopTimer(); goTo(current - 1); startTimer(); });
      nextBtn.addEventListener('click', () => { stopTimer(); goTo(current + 1); startTimer(); });

      // Dot buttons
      dots.forEach((dot, i) => {
        dot.addEventListener('click', () => { stopTimer(); goTo(i); startTimer(); });
      });

      // Pause on hover
      const carousel = track.closest('.hero-carousel');
      if (carousel) {
        carousel.addEventListener('mouseenter', stopTimer);
        carousel.addEventListener('mouseleave', startTimer);
      }

      // Touch / swipe support
      let touchStartX = 0;
      track.addEventListener('touchstart', (e) => {
        touchStartX = e.touches[0].clientX;
      }, { passive: true });
      track.addEventListener('touchend', (e) => {
        const delta = e.changedTouches[0].clientX - touchStartX;
        if (Math.abs(delta) > 40) {
          stopTimer();
          goTo(delta < 0 ? current + 1 : current - 1);
          startTimer();
        }
      }, { passive: true });

    }());
  </script>
```

**Key implementation notes:**
- `goTo()` derives slide count from `slides.length` (DOM-driven per D-03 — never hardcoded).
- `((n % total) + total) % total` handles negative modulo (loop backwards per D-09).
- `translateX(-${current * 100}%)` works because each slide is `flex: 0 0 240px` and the track-wrap is also `240px` — one slide width = 100% of the visible window.
- Touch delta threshold is 40px (reasonable minimum for an intentional swipe, per D-13).
- `{ passive: true }` on touch listeners avoids browser scroll-blocking warnings.
- The carousel IIFE goes in its own `<script>` tag (consistent with existing pattern of one concern per script block).
  </action>
  <verify>
    <automated>
      # New carousel script block present
      grep -c 'Hero carousel' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # goTo function defined
      grep -c 'function goTo' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # setInterval (auto-advance)
      grep -c 'setInterval' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Touch event listeners
      grep -c "addEventListener.*touchstart" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Mouseenter / mouseleave pause
      grep -c 'mouseenter' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # translateX (CSS transform drive)
      grep -c 'translateX' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # dot--active toggled in JS
      grep -c 'hero-carousel__dot--active' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # No hardcoded slide count (4 should not appear as a literal in goTo logic)
      # Confirm total derived from slides.length
      grep -c 'slides\.length' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\/\* ── Hero carousel ── \*\/' index.html` returns 2 (one in CSS, one in the new script block)
    - `grep -c 'function goTo' index.html` returns 1
    - `grep -c 'setInterval' index.html` returns 1
    - `grep -c 'touchstart' index.html` returns 1
    - `grep -c 'touchend' index.html` returns 1
    - `grep -c 'mouseenter' index.html` returns 1
    - `grep -c 'mouseleave' index.html` returns 1
    - `grep -c 'translateX' index.html` returns at least 1 (in the JS goTo function)
    - `grep -c 'slides\.length' index.html` returns 1 (slide count is DOM-derived, not hardcoded)
    - `grep -c '4000' index.html` returns 1 (4-second auto-advance interval per D-06)
    - `grep -c 'passive.*true' index.html` returns 2 (both touchstart and touchend listeners use passive:true)
  </acceptance_criteria>
  <done>
    The carousel JS IIFE is in its own `<script>` block before `</body>`. It reads slide count from the DOM, advances with translateX, pauses on mouseenter, loops, supports touch swipe with a 40px threshold, and toggles `.hero-carousel__dot--active` on the correct dot at each index.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete hero carousel: 4 slides (Connect, Scan, Preview & Export, Results) displayed one at a time inside the phone bezel, with prev/next circular arrow buttons flanking the phone, dot indicators below, 4-second auto-advance, mouseenter pause, loop, and left/right swipe on mobile.
  </what-built>
  <how-to-verify>
    Open `index.html` directly in a browser (File → Open, or `open index.html` in terminal).

    **Desktop checks:**
    1. Hero section shows ONE phone bezel with the "Connect" screenshot visible. No fan strip.
    2. Two circular arrow buttons (chevron left / chevron right) appear flanking the phone.
    3. Four dots appear below the phone. The first dot is filled blue (--accent); others are grey.
    4. Click the right arrow — phone slides to "Scan". Second dot turns blue.
    5. Click the right arrow twice more — "Preview & Export" then "Results" appear.
    6. Click the right arrow again — loops back to "Connect". Dots update correctly each time.
    7. Click the left arrow from "Connect" — should loop to "Results".
    8. Wait 4 seconds without touching — carousel auto-advances to the next slide.
    9. Hover over the carousel — auto-advance pauses. Move cursor away — it resumes.
    10. Click a dot directly — jumps to that slide.

    **Mobile checks (use browser DevTools device simulation at 375px width):**
    11. Arrow buttons are still visible (not hidden) — may be slightly smaller (36px per mobile CSS).
    12. Swipe left on the phone area — advances to the next slide.
    13. Swipe right — goes back to the previous slide.

    **Accessibility check (browser DevTools → Accessibility tab):**
    14. The `.hero-carousel` region has `aria-label="App screenshot carousel"`.
    15. Each slide has `role="group"` and `aria-label` with its position ("1 of 4: Connect").
    16. Prev/next buttons have accessible `aria-label` ("Previous slide" / "Next slide").
  </how-to-verify>
  <resume-signal>Type "approved" if carousel works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Touch events → JS handler | User swipe input; no data leaves the browser |
| Static file → browser | index.html is a static asset; carousel JS has no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | setInterval auto-advance | accept | Interval runs in-tab only; no server resource; tab-close clears it automatically. |
| T-02-04 | Tampering | Carousel state (current index) | accept | In-memory JS variable; no persistence; no integrity concern. |
| T-02-05 | Information Disclosure | Touch event coordinates | accept | touchstart/touchend read only clientX for swipe delta; no data stored or transmitted. |
| T-02-06 | Elevation of Privilege | Inline script | accept | No eval, no user-controlled script execution. Content-Security-Policy for inline scripts is a static-host concern (GitHub Pages default); no injection vector exists in this feature. |
</threat_model>

<verification>
Human verification checkpoint (Task 3) confirms the carousel works end-to-end: single-slide display, arrow navigation, dot indicators, auto-advance, hover pause, loop, and touch swipe. All four screenshots are visible when cycling through slides.
</verification>

<success_criteria>
- Hero section shows one phone at a time — fan strip is gone
- All four slides render: Connect, Scan, Preview & Export, Results (HERO-02)
- Prev/next arrows advance the carousel and loop at ends (HERO-01, HERO-03)
- Dot indicators reflect the current slide (HERO-03)
- Auto-advance fires at 4-second intervals and pauses on mouseenter (D-06, D-07)
- Touch swipe on mobile advances the carousel (HERO-03, D-13)
- Adding a new `.hero-carousel__slide` div to the HTML is sufficient to extend the carousel — no JS constant change needed (D-03)
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-carousel/02-02-SUMMARY.md` using the summary template.
</output>
