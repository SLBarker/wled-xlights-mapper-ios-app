---
phase: 01-mobile-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - MOBL-03
  - MOBL-04
  - MOBL-05

must_haves:
  truths:
    - "Every .phone-bezel__screen element contains exactly one .phone-bezel__notch (no doubled notch)"
    - "Every .phone-bezel has exactly one .phone-bezel__screen as a direct child of .phone-bezel__frame (no nesting)"
    - "Each of the three hero phone images has a unique, accurate alt text description"
    - "Both video elements have unique, accurate aria-label text"
    - "The export section phone image has an accurate alt text"
    - "The word 'its' (possessive) is used throughout Step 04 copy — no contraction 'it's' remains in that section"
  artifacts:
    - path: "index.html"
      provides: "All markup bug fixes"
      contains: "App screenshot showing the WLED controller connect screen with mDNS discovery list"
  key_links:
    - from: "index.html hero phone 1"
      to: "correct DOM structure"
      via: "single phone-bezel__screen with one notch"
      pattern: "phone-bezel__screen"
    - from: "Step 04 paragraph"
      to: "corrected possessive"
      via: "text replacement"
      pattern: "its position"
---

<objective>
Fix all markup bugs in index.html: repair five instances of nested phone-bezel__screen DOM structure, update four incorrect or duplicated alt/aria-label text values, and correct two "it's"→"its" possessive errors in Step 04 copy.

Purpose: These are pre-existing defects visible to users — double-border phone bezels, screen reader misinformation from wrong alt text, and grammatically incorrect copy. They must be fixed before responsive work begins.
Output: index.html with corrected DOM structure, accurate alt/aria-label text, and corrected Step 04 copy.
</objective>

<execution_context>
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-mobile-and-polish/01-UI-SPEC.md

<interfaces>
<!-- Correct phone-bezel DOM structure (from UI-SPEC.md §MOBL-03) -->
<!--
.phone-bezel
  .phone-bezel__frame
    .phone-bezel__screen
      .phone-bezel__notch [aria-hidden="true"]
      <img> or <video>
  .phone-bezel__caption
-->

<!-- Correct alt texts (from UI-SPEC.md §MOBL-04) -->
<!--
connect.jpeg  → "App screenshot showing the WLED controller connect screen with mDNS discovery list"
ar.jpeg       → "App screenshot showing the AR scanning view with gray-code pattern detection overlay"
preview.jpeg  → "App screenshot showing the 3D LED preview with quality tier colour overlay"
results.jpeg  → implied: give it an accurate description of the results/export screen
-->

<!-- Correct video aria-labels (from UI-SPEC.md §MOBL-04) -->
<!--
discovery.mp4   → "Screen recording of automatic mDNS controller discovery and segment selection"
3dpreview.mp4   → "Screen recording of interactive 3D LED preview with pan, zoom, and rotate controls"
-->

<!-- Step 04 grammar (from UI-SPEC.md §MOBL-05) -->
<!--
Current (wrong): "Use the transform gizmo to adjust it's position."
                 "Delete the calculated position and replace with an interpolated position based on it's neighbours."
Correct:         "Use the transform gizmo to adjust its position."
                 "Delete the calculated position and replace with an interpolated position based on its neighbours."
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nested phone-bezel__screen DOM — five instances</name>
  <files>index.html</files>

  <read_first>
    - index.html (full file — executor must read before any edits to locate all five instances)
    - .planning/phases/01-mobile-and-polish/01-UI-SPEC.md §MOBL-03 (correct DOM structure contract)
  </read_first>

  <action>
Locate and fix all five instances of the nested `.phone-bezel__screen` bug. In each case, the outer `.phone-bezel__screen` element wraps an inner `.phone-bezel__screen`, creating a double-border artefact.

The correct structure for every phone-bezel is:

```html
<div class="phone-bezel">
  <div class="phone-bezel__frame">
    <div class="phone-bezel__screen">
      <div class="phone-bezel__notch" aria-hidden="true"></div>
      <!-- img or video here -->
    </div>
  </div>
  <p class="phone-bezel__caption">...</p>  <!-- if present -->
</div>
```

Rules:
- Remove all inner `.phone-bezel__screen` divs — keep only the outermost one.
- Each `.phone-bezel__screen` must contain exactly ONE `.phone-bezel__notch`. Keep the one with `aria-hidden="true"`. Remove the duplicate notch that lacks `aria-hidden="true"`.
- The `<img>` or `<video>` element moves to be a direct sibling of `.phone-bezel__notch` inside the single `.phone-bezel__screen`.

The five affected locations in the current HTML (identified by context — read the file to confirm exact line numbers before editing):
1. Hero phone 1 (connect.jpeg) — nested screen wraps img
2. Hero phone 3 (preview.jpeg) — nested screen wraps img
3. Features section, discovery feature (discovery.mp4 video) — nested screen wraps video
4. Features section, 3D preview feature (3dpreview.mp4 video) — nested screen wraps video
5. Export section phone (results.jpeg) — nested screen wraps img

After fixing, the `<video>` elements must retain ALL their existing attributes: `autoplay loop muted playsinline style="width:100%; height:100%; object-fit:cover; display:block;"`. Only the surrounding nesting structure changes.
  </action>

  <verify>
    <automated>grep -c "phone-bezel__screen" index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "class=\"phone-bezel__screen\"" index.html` returns exactly 9 (one per phone-bezel: 3 hero + 2 features inline + 1 export + 2 features placeholder + scan placeholder = verify exact count by reading fixed file — should have NO instance where one phone-bezel__screen contains another phone-bezel__screen)
    - `grep -A5 "phone-bezel__screen" index.html | grep "phone-bezel__screen"` returns no back-to-back matches (i.e., no nested screens remain)
    - All five video/img elements remain present with their original src attributes intact
    - The `<video>` elements retain `autoplay loop muted playsinline` attributes
    - Every `.phone-bezel__notch` remaining in the file has `aria-hidden="true"`
  </acceptance_criteria>

  <done>No phone-bezel__screen element contains another phone-bezel__screen element. Every phone-bezel has exactly one screen with exactly one notch (aria-hidden="true") and one media element (img or video).</done>
</task>

<task type="auto">
  <name>Task 2: Fix alt text and aria-labels, correct Step 04 grammar</name>
  <files>index.html</files>

  <read_first>
    - index.html (re-read after Task 1 is complete, since Task 1 modifies the file)
    - .planning/phases/01-mobile-and-polish/01-UI-SPEC.md §MOBL-04 and §MOBL-05
  </read_first>

  <action>
Make the following targeted text replacements in index.html:

**Alt text fixes (MOBL-04) — three hero phone images:**

1. connect.jpeg alt text:
   - FROM: `alt="App screenshot showing the controller connect screen with mDNS discovery list"`
   - TO:   `alt="App screenshot showing the WLED controller connect screen with mDNS discovery list"`
   (Add "WLED" before "controller")

2. ar.jpeg alt text:
   - FROM: `alt="App screenshot showing the controller connect screen with mDNS discovery list"`
   - TO:   `alt="App screenshot showing the AR scanning view with gray-code pattern detection overlay"`

3. preview.jpeg alt text:
   - FROM: `alt="App screenshot showing the controller connect screen with mDNS discovery list"`
   - TO:   `alt="App screenshot showing the 3D LED preview with quality tier colour overlay"`

4. results.jpeg alt text (export section):
   - FROM: `alt="App screenshot showing the controller connect screen with mDNS discovery list"`
   - TO:   `alt="App screenshot showing the xmodel export results screen with quantisation metrics"`

**Video aria-label fixes (MOBL-04) — two video elements:**

5. discovery.mp4 aria-label:
   - FROM: `aria-label="Screen recording of the AR scan in progress, showing gray-code pattern cycling across LEDs"`
   - TO:   `aria-label="Screen recording of automatic mDNS controller discovery and segment selection"`

6. 3dpreview.mp4 aria-label:
   - FROM: `aria-label="Screen recording of the AR scan in progress, showing gray-code pattern cycling across LEDs"`
   - TO:   `aria-label="Screen recording of interactive 3D LED preview with pan, zoom, and rotate controls"`

**Grammar fixes (MOBL-05) — Step 04 paragraph only:**

7. First occurrence in Step 04 `<p>` elements:
   - FROM: `adjust it's position`
   - TO:   `adjust its position`

8. Second occurrence in Step 04 `<p>` elements:
   - FROM: `based on it's neighbours`
   - TO:   `based on its neighbours`

Note: Only fix "it's" → "its" inside the Step 04 step-card. Do not alter any other text on the page.
  </action>

  <verify>
    <automated>grep -n "it's" index.html | grep -v "^#"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "it's" index.html` returns 0 (no contraction remains anywhere in the file)
    - `grep "ar.jpeg" index.html` contains the text `AR scanning view with gray-code pattern detection overlay`
    - `grep "preview.jpeg" index.html` contains the text `3D LED preview with quality tier colour overlay`
    - `grep "connect.jpeg" index.html` contains the text `WLED controller connect screen`
    - `grep "results.jpeg" index.html` does NOT contain `controller connect screen`
    - `grep "discovery.mp4" index.html` contains the text `automatic mDNS controller discovery`
    - `grep "3dpreview.mp4" index.html` contains the text `interactive 3D LED preview with pan, zoom`
    - `grep "controller connect screen" index.html` returns exactly 1 line (connect.jpeg only)
  </acceptance_criteria>

  <done>All four images have unique, accurate alt text. Both videos have unique, accurate aria-labels. Step 04 copy uses "its" (possessive) throughout. No other text on the page has been altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML served to browser | No dynamic content, no user input, no server in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | alt/aria-label text | accept | Text corrections improve accuracy; no sensitive data exposed |
| T-01-02 | Tampering | index.html file | accept | Static file on GitHub Pages; no write access from client; content integrity via git history |
</threat_model>

<verification>
After both tasks complete, open index.html in a browser and verify:
1. Hero phones — none show a double-border artefact (the inner nested frame is gone)
2. Inspect the DOM: each `.phone-bezel__frame` has exactly one `.phone-bezel__screen` child
3. Inspect alt text via browser DevTools on the three hero images — each is unique and accurate
4. Step 04 "Preview" card reads: "Use the transform gizmo to adjust its position." (no apostrophe)
</verification>

<success_criteria>
- Zero nested .phone-bezel__screen elements remain (no element of class phone-bezel__screen is a descendant of another)
- Four images have unique, factually accurate alt text — none share the "controller connect screen" description except connect.jpeg
- Both videos have unique aria-labels describing their actual content
- "it's" does not appear anywhere in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-and-polish/01-01-SUMMARY.md` using the summary template at `/Users/simonbarker/.claude/get-shit-done/templates/summary.md`
</output>
