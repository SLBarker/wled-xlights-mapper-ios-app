---
phase: 03-contact-section
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "A `/* ── Contact ── */` CSS block exists in the <style> tag, inserted immediately before `/* ── Footer ── */`"
    - "`.contact-cta` has scroll-reveal initial state (opacity 0, translateY 16px, transition)"
    - "`.contact-cta.visible` has the revealed state (opacity 1, translateY 0)"
    - "`#contact` rule centers content with `text-align: center`"
  artifacts:
    - path: "index.html"
      provides: "Contact CSS block with scroll-reveal rules"
      contains: "#contact { text-align: center; }"
  key_links:
    - from: ".contact-cta CSS class"
      to: "IntersectionObserver .visible toggling (Plan 03-02)"
      via: "shared class name `.contact-cta`"
      pattern: "contact-cta"
---

<objective>
Add the Contact section CSS block to index.html's inline `<style>` tag.

Purpose: Establishes all styling the Contact section needs before the HTML exists. Because index.html uses a single inline stylesheet, CSS must land in the correct position (before `/* ── Footer ── */`) to keep the cascade ordered and the file readable.

Output: Three new CSS rules in index.html — `#contact` (centering), `.contact-cta` (scroll-reveal initial state), `.contact-cta.visible` (revealed state).
</objective>

<execution_context>
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/PROJECT.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/ROADMAP.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/STATE.md

<interfaces>
<!-- Existing CSS patterns the executor must follow. Extracted from index.html. -->

Existing scroll-reveal pattern (apply to `.contact-cta`):
```css
/* Elements start hidden and slide up into view */
opacity: 0;
transform: translateY(16px);
transition: opacity 0.5s ease, transform 0.5s ease;

/* .visible class added by IntersectionObserver */
opacity: 1;
transform: translateY(0);
```

Existing section comment banner format:
```css
/* ── Features ── */
/* ── Privacy ── */
/* ── Footer ── */   ← insert Contact block immediately BEFORE this line
```

Existing `.section` default (no background override needed — `.section` already uses `--bg`):
```css
.section {
  padding: 100px 2rem;
  /* no explicit background — inherits --bg from body */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Contact CSS block before `/* ── Footer ── */`</name>
  <files>index.html</files>

  <read_first>
    - index.html lines 830–845 — confirm exact text of `/* ── Footer ── */` comment and surrounding context before editing
  </read_first>

  <action>
Open index.html. Find the line containing `/* ── Footer ── */` (approximately line 838). Insert the following CSS block on the lines immediately before it, preserving one blank line between the new block and the Footer comment:

```css
/* ── Contact ── */
#contact { text-align: center; }

.contact-cta {
  margin-top: 2rem;
  opacity: 0;
  transform: translateY(16px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}

.contact-cta.visible {
  opacity: 1;
  transform: translateY(0);
}

```

The result in the file should read:

```
.contact-cta.visible {
  opacity: 1;
  transform: translateY(0);
}

/* ── Footer ── */
```

Do NOT change any other lines. Do NOT add background-color to `#contact` — `.section` already provides the light `--bg` background.
  </action>

  <verify>
    <automated>grep -n "contact-cta" index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "contact-cta" index.html` returns 3 (the rule, the transition block, and the .visible rule)
    - `grep -n "\/\* ── Contact ── \*\/" index.html` returns a line number that is less than the line number returned by `grep -n "\/\* ── Footer ── \*\/" index.html`
    - `grep -n "#contact { text-align: center; }" index.html` returns exactly one match
    - `grep -n "transform: translateY(16px)" index.html` returns a match inside the `.contact-cta` block (line between the `/* ── Contact ── */` and `/* ── Footer ── */` markers)
    - `grep -n "opacity: 1;" index.html` includes a match inside `.contact-cta.visible`
    - No line in the file reads `background` inside a `#contact` rule (no background override added)
  </acceptance_criteria>

  <done>
    The Contact CSS block (3 rules: `#contact`, `.contact-cta`, `.contact-cta.visible`) exists in index.html immediately before the `/* ── Footer ── */` comment. File is otherwise unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS-only change | No user input processed; no data flow; purely presentational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | index.html CSS block | accept | Static file served via GitHub Pages; no server-side processing; CSS insertion poses no security risk |
</threat_model>

<verification>
After task completes:

```bash
grep -n "contact-cta" index.html
grep -n "/* ── Contact ── */" index.html
grep -n "/* ── Footer ── */" index.html
```

Confirm Contact CSS comment line number < Footer CSS comment line number.
</verification>

<success_criteria>
- `grep "contact-cta" index.html` returns 3 lines
- `grep "#contact { text-align: center; }" index.html` returns 1 line
- Contact CSS block appears before Footer CSS block in the file
- File still opens without HTML/CSS parse errors (validate by opening in browser)
</success_criteria>

<output>
After completion, create `.planning/phases/03-contact-section/03-01-SUMMARY.md`
</output>
