---
phase: 03-contact-section
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - index.html
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "A Contact section (`id=\"contact\"`) is visible on the page between #privacy and <footer>"
    - "The section heading reads exactly 'Share your feedback'"
    - "The section label (eyebrow) reads exactly 'Contact'"
    - "A `Send Feedback` link exists with class `btn btn-primary contact-cta`"
    - "The href of that link is exactly `mailto:wled.2.xlights@gmail.com?subject=Feedback%20for%20WLED%20xLights%20Mapper`"
    - "A `Contact` nav link (`href='#contact'`) exists in both `.site-nav__links` and `.site-nav__drawer`"
    - "The IntersectionObserver querySelectorAll selector includes `.contact-cta`"
  artifacts:
    - path: "index.html"
      provides: "Contact section HTML, nav links, updated scroll-reveal selector"
      contains: "id=\"contact\""
  key_links:
    - from: "`.site-nav__links` li"
      to: "`#contact` section"
      via: "`href='#contact'`"
      pattern: "href=\"#contact\""
    - from: "`.site-nav__drawer` li"
      to: "`#contact` section"
      via: "`href='#contact'`"
      pattern: "href=\"#contact\""
    - from: "IntersectionObserver querySelectorAll"
      to: "`.contact-cta` element"
      via: "CSS class selector in selector string"
      pattern: "contact-cta"
    - from: "`<a class=\"btn btn-primary contact-cta\">`"
      to: "mail client"
      via: "mailto: URI"
      pattern: "mailto:wled.2.xlights@gmail.com"
---

<objective>
Add the Contact HTML section, two nav links, and the scroll-reveal selector update to index.html.

Purpose: Delivers all three user-visible changes: a new section users can scroll to, nav links that jump to it, and scroll-reveal animation that fires when the CTA enters the viewport. This plan satisfies CONT-01 (styled section), CONT-02 (mailto CTA), and CONT-03 (pre-filled subject).

Output: Three edits to index.html — Contact `<section>` block, two `<li>` nav link insertions, and one updated `querySelectorAll` string.
</objective>

<execution_context>
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/PROJECT.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/ROADMAP.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/STATE.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/phases/03-contact-section/03-01-SUMMARY.md

<interfaces>
<!-- Existing HTML patterns extracted from index.html. Executor uses these directly. -->

Section markup pattern (match exactly):
```html
<section id="{id}" class="section" aria-labelledby="{id}-title">
  <div class="container">
    <span class="section-label">…</span>
    <h2 class="section-title" id="{id}-title">…</h2>
    <p class="section-body">…</p>
  </div>
</section>
```

Existing desktop nav list (lines ~985–993):
```html
<ul class="site-nav__links">
  <li><a href="#features">Features</a></li>
  <li><a href="#how-it-works">How It Works</a></li>
  <li><a href="#requirements">Requirements</a></li>
  <li><a href="#tips">Tips</a></li>
  <li><a href="#privacy">Privacy</a></li>    ← add Contact AFTER this line
  <!-- Download CTA follows -->
</ul>
```

Existing mobile nav drawer list (lines ~1012–1020):
```html
<div class="site-nav__drawer" …>
  <ul>
    <li><a href="#features">Features</a></li>
    <li><a href="#how-it-works">How It Works</a></li>
    <li><a href="#requirements">Requirements</a></li>
    <li><a href="#tips">Tips</a></li>
    <li><a href="#privacy">Privacy</a></li>    ← add Contact AFTER this line
    <!-- Download CTA follows -->
  </ul>
</div>
```

Existing IntersectionObserver querySelectorAll (lines ~1558–1559):
```javascript
document.querySelectorAll('.feature-row, .feature-card, .step-card, .req-card, .tip-item, .privacy-card')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Contact nav links to desktop and mobile nav</name>
  <files>index.html</files>

  <read_first>
    - index.html lines 982–1025 — confirm exact current state of both nav lists before editing
  </read_first>

  <action>
Make two edits to index.html, both inserting the same new list item:

**Edit 1 — Desktop nav** (`.site-nav__links`):
Find the line `<li><a href="#privacy">Privacy</a></li>` inside the `.site-nav__links` `<ul>`. Insert immediately after it (on the next line, preserving indentation):

```html
              <li><a href="#contact">Contact</a></li>
```

**Edit 2 — Mobile nav drawer** (`.site-nav__drawer`):
Find the second occurrence of `<li><a href="#privacy">Privacy</a></li>` (the one inside `.site-nav__drawer`). Insert immediately after it (on the next line, preserving indentation):

```html
              <li><a href="#contact">Contact</a></li>
```

Do NOT modify any other lines. Do NOT change the Download CTA items.
  </action>

  <verify>
    <automated>grep -n "href=\"#contact\"" index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "href=\"#contact\"" index.html` returns at least 3 (2 nav occurrences + 1 in the section itself, added in Task 2 — but at this stage after Task 1 only, returns 2)
    - After Task 1 alone: `grep -c "href=\"#contact\"" index.html` returns exactly 2
    - Both occurrences of `href="#contact"` appear before line 1100 (i.e., inside the nav, not the section body)
    - The line `<li><a href="#contact">Contact</a></li>` appears after `<li><a href="#privacy">Privacy</a></li>` in both nav lists
    - `grep -n "href=\"#privacy\"" index.html` still returns exactly 2 lines (unchanged)
  </acceptance_criteria>

  <done>
    Two `<li><a href="#contact">Contact</a></li>` items exist in index.html — one in `.site-nav__links`, one in `.site-nav__drawer`. No other nav markup changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Contact HTML section and update scroll-reveal selector</name>
  <files>index.html</files>

  <read_first>
    - index.html lines 1540–1580 — confirm exact position of `</section>` (end of #privacy), `</main>`, footer, and IntersectionObserver querySelectorAll before editing
  </read_first>

  <action>
Make two edits to index.html:

**Edit 1 — Contact section HTML:**
Find the closing `</section>` tag that ends the `#privacy` section (approximately line 1546 — it is the `</section>` immediately followed by `</main>`). Insert the following block between that `</section>` and the `</main>` tag:

```html

    <!-- ══ Contact ══ -->
    <section id="contact" class="section" aria-labelledby="contact-title">
      <div class="container">
        <span class="section-label">Contact</span>
        <h2 class="section-title" id="contact-title">Share your feedback</h2>
        <p class="section-body">Found a bug or have a feature request? I'd love to hear from you.</p>
        <a href="mailto:wled.2.xlights@gmail.com?subject=Feedback%20for%20WLED%20xLights%20Mapper"
           class="btn btn-primary contact-cta">Send Feedback</a>
      </div>
    </section>
```

The file structure after the edit must read:
```
    </section>   ← end of #privacy

    <!-- ══ Contact ══ -->
    <section id="contact" class="section" aria-labelledby="contact-title">
      …
    </section>

  </main>
```

**Edit 2 — Scroll-reveal selector:**
Find the `document.querySelectorAll(…)` call in the IntersectionObserver block (approximately line 1558). The current selector string ends with `'.feature-row, .feature-card, .step-card, .req-card, .tip-item, .privacy-card'`.

Replace that selector string with:
```
'.feature-row, .feature-card, .step-card, .req-card, .tip-item, .privacy-card, .contact-cta'
```

Only the selector string changes. Do NOT alter any other part of the IntersectionObserver block.
  </action>

  <verify>
    <automated>grep -n "contact" index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "id=\"contact\"" index.html` returns exactly 1
    - `grep -n "Share your feedback" index.html` returns exactly 1 line
    - `grep -n "section-label.*Contact\|Contact.*section-label" index.html` returns 0 (the eyebrow text is in a `<span class="section-label">`, check instead with: `grep -n ">Contact<" index.html` returns exactly 1 match inside the Contact section, not in the nav)
    - `grep -n "mailto:wled.2.xlights@gmail.com" index.html` returns exactly 1 line
    - That line contains the full string `mailto:wled.2.xlights@gmail.com?subject=Feedback%20for%20WLED%20xLights%20Mapper`
    - `grep -n "Send Feedback" index.html` returns exactly 1 line containing `class="btn btn-primary contact-cta"`
    - `grep -n "btn btn-primary contact-cta" index.html` returns exactly 1 line with an `<a` tag (not `<button`)
    - `grep -n "contact-cta" index.html` returns at least 4 lines: CSS rule, CSS transition block, CSS .visible rule (from Plan 03-01), the `<a>` tag, and the updated querySelectorAll selector
    - `grep -n "privacy-card, .contact-cta" index.html` returns exactly 1 line (the updated querySelectorAll)
    - `grep -n "</main>" index.html` returns the same line count as before (no duplicate `</main>` added)
    - `grep -n "aria-labelledby=\"contact-title\"" index.html` returns exactly 1 line
  </acceptance_criteria>

  <done>
    The Contact section HTML block is present in index.html between `#privacy`'s closing `</section>` and `</main>`. The IntersectionObserver querySelectorAll selector includes `.contact-cta`. All locked values from D-01 through D-10 are in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mailto: URI | User's email client is invoked with a pre-filled address and subject. No data enters the server. |
| Static HTML | index.html is served verbatim by GitHub Pages; no template rendering or user input injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | `mailto:` recipient address | accept | `wled.2.xlights@gmail.com` is a dedicated support address, intentionally public (per D-01). Exposure is desired. |
| T-03-02-02 | Tampering | Pre-filled subject via `mailto:` | accept | Subject is percent-encoded and hardcoded; no user-controlled input in the URL. Email client opens with subject pre-filled but user can edit before sending — intended behavior. |
| T-03-02-03 | Spoofing | Mailto link injection | mitigate | The href is a string literal in static HTML (not constructed from user input); no injection vector exists. Verified by acceptance criteria grep on exact href string. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm nav links
grep -c "href=\"#contact\"" index.html        # expect 3 (2 nav + 1 section anchor)

# Confirm section exists
grep -n "id=\"contact\"" index.html           # expect 1 match

# Confirm locked content values
grep -n "Share your feedback" index.html      # expect 1 match
grep -n "Send Feedback" index.html            # expect 1 match
grep -n "mailto:wled.2.xlights@gmail.com" index.html  # expect 1 match with full subject param

# Confirm scroll-reveal updated
grep -n "contact-cta" index.html              # expect 4+ matches (CSS + HTML + querySelectorAll)

# Confirm no structural damage
grep -c "</main>" index.html                  # expect 1
grep -c "</section>" index.html               # count should be 1 more than before Phase 3 (new section added)
```

Open index.html in a browser and verify:
- "Contact" appears in both desktop nav and mobile hamburger drawer
- Scrolling to the bottom reveals the Contact section between Privacy and the footer
- Clicking "Send Feedback" opens the email client addressed to `wled.2.xlights@gmail.com` with subject `Feedback for WLED xLights Mapper`
- Section has light background (matches Features/Tips sections visually)
- CTA button slides up into view on scroll (scroll-reveal animation fires)
</verification>

<success_criteria>
- Contact section is visible in the browser at the bottom of the page, above the footer
- Section heading reads "Share your feedback", eyebrow reads "Contact"
- "Send Feedback" `<a>` element with class `btn btn-primary contact-cta` opens email client with correct address and subject when clicked
- "Contact" nav link appears in desktop nav bar and mobile drawer, scrolls to `#contact`
- `.contact-cta` animates in with scroll-reveal when section enters viewport
- All locked decisions D-01 through D-10 are implemented exactly as specified
</success_criteria>

<output>
After completion, create `.planning/phases/03-contact-section/03-02-SUMMARY.md`
</output>
