---
phase: 10-font-self-hosting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/fonts/inter-400.woff2
  - resources/fonts/inter-500.woff2
  - resources/fonts/inter-600.woff2
  - resources/fonts/inter-700.woff2
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "All 4 Inter weights (400, 500, 600, 700) exist as local WOFF2 files"
    - "Font files live under resources/fonts/ at repo root"
  artifacts:
    - path: "resources/fonts/inter-400.woff2"
      provides: "Inter Regular weight, Latin subset, WOFF2"
    - path: "resources/fonts/inter-500.woff2"
      provides: "Inter Medium weight, Latin subset, WOFF2"
    - path: "resources/fonts/inter-600.woff2"
      provides: "Inter SemiBold weight, Latin subset, WOFF2"
    - path: "resources/fonts/inter-700.woff2"
      provides: "Inter Bold weight, Latin subset, WOFF2"
  key_links:
    - from: "resources/fonts/"
      to: "index.html @font-face declarations (Plan 10-02)"
      via: "src: url('resources/fonts/inter-NNN.woff2')"
      pattern: "inter-(400|500|600|700)\\.woff2"
---

<objective>
Obtain the 4 Inter Latin-subset WOFF2 font files (weights 400, 500, 600, 700) and commit them to a new `resources/fonts/` directory.

Purpose: Self-hosted font binaries are the prerequisite for removing the Google Fonts CDN dependency (PERF-02). The HTML wiring in Plan 10-02 references these exact file paths.
Output: 4 WOFF2 files in `resources/fonts/` with flat naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-font-self-hosting/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resources/fonts/ and download Inter Latin WOFF2 files</name>
  <files>resources/fonts/inter-400.woff2, resources/fonts/inter-500.woff2, resources/fonts/inter-600.woff2, resources/fonts/inter-700.woff2</files>
  <read_first>
    - .planning/phases/10-font-self-hosting/10-CONTEXT.md (D-01 through D-05, font source guidance)
    - CLAUDE.md (resources/ asset pattern, GitHub Pages static hosting constraint)
  </read_first>
  <action>
    Create the directory `resources/fonts/` at the repo root (sibling of the existing `resources/screenshot/`, `resources/video/`, `resources/icons/` directories).

    Download the 4 Inter Latin-subset WOFF2 files using `curl -L -o`. Use the `google-webfonts-helper` API, which serves the exact Latin-subset WOFF2 files Google Fonts produces. The download URLs follow this pattern (download endpoint returns a single WOFF2 per weight/subset):

    - Weight 400 -> save as `resources/fonts/inter-400.woff2`
      URL: `https://gwfh.mranftl.com/api/fonts/inter?download=woff2&subsets=latin` and extract the `400` file, OR use the per-file URL `https://gwfh.mranftl.com/fonts/inter/v13/inter-v13-latin-regular.woff2`
    - Weight 500 -> save as `resources/fonts/inter-500.woff2`
      Per-file URL: `https://gwfh.mranftl.com/fonts/inter/v13/inter-v13-latin-500.woff2`
    - Weight 600 -> save as `resources/fonts/inter-600.woff2`
      Per-file URL: `https://gwfh.mranftl.com/fonts/inter/v13/inter-v13-latin-600.woff2`
    - Weight 700 -> save as `resources/fonts/inter-700.woff2`
      Per-file URL: `https://gwfh.mranftl.com/fonts/inter/v13/inter-v13-latin-700.woff2`

    If the `gwfh.mranftl.com` per-file URLs return a non-200 or the version path (`v13`) has changed, fall back: call `curl -s "https://gwfh.mranftl.com/api/fonts/inter?subsets=latin"` to read the current variant URLs from the JSON `variants` array (each variant has a `woff2` field), then download the `regular`, `500`, `600`, `700` variant WOFF2 URLs. Do NOT download weight 300 (Light) — D-01 drops it. Do NOT download non-latin subsets — D-02 is Latin only. Save each file with the exact flat names listed above (no version suffix, no subfolder).
  </action>
  <verify>
    <automated>test -f resources/fonts/inter-400.woff2 && test -f resources/fonts/inter-500.woff2 && test -f resources/fonts/inter-600.woff2 && test -f resources/fonts/inter-700.woff2 && file resources/fonts/inter-400.woff2 | grep -qi 'Web Open Font Format'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `ls resources/fonts/` lists exactly 4 files: inter-400.woff2, inter-500.woff2, inter-600.woff2, inter-700.woff2
    - CLI: `file resources/fonts/inter-700.woff2` output contains "Web Open Font Format (Version 2)" — confirms valid WOFF2, not an HTML error page
    - Behavior: each file size is between 15KB and 60KB (`wc -c < resources/fonts/inter-400.woff2` returns a value in 15000..60000) — Latin subset is ~25-30KB; a value near 0 or several hundred KB indicates a wrong/full-Unicode download
    - Source assertion: no file named `inter-300.woff2` exists in resources/fonts/ (`test ! -f resources/fonts/inter-300.woff2`)
  </acceptance_criteria>
  <done>resources/fonts/ contains exactly 4 valid WOFF2 files (inter-400/500/600/700.woff2), each a Latin-subset binary in the 15-60KB range, with no weight-300 file.</done>
</task>

</tasks>

<verification>
- All 4 WOFF2 files present in resources/fonts/ and identified as WOFF2 binaries by `file`.
- File sizes plausible for Latin subset (~25-30KB each).
- No weight-300 file present.
</verification>

<success_criteria>
- `resources/fonts/` directory exists at repo root.
- Contains inter-400.woff2, inter-500.woff2, inter-600.woff2, inter-700.woff2 — all valid WOFF2.
- Ready for Plan 10-02 to reference via @font-face.
</success_criteria>

<output>
Create `.planning/phases/10-font-self-hosting/10-01-SUMMARY.md` when done.
</output>
