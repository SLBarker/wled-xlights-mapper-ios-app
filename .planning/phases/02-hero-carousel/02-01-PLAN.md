---
phase: 02-hero-carousel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03

must_haves:
  truths:
    - "The old .hero__phones fan-strip CSS is gone — no fan strip can render"
    - "Carousel CSS section exists with .hero-carousel, track, slide, arrow, dots, and dot classes"
    - "Arrow buttons styled with --surface, --accent, --radius-lg, --shadow tokens"
    - "Active dot uses --accent fill and .hero-carousel__dot--active class"
    - "Mobile responsive rules exist for carousel (not for the old .hero__phones)"
    - "Tilted-phone transforms (.hero__phones .phone-bezel:nth-child) are removed"
  artifacts:
    - path: "index.html"
      provides: "All carousel CSS"
      contains: ".hero-carousel"
  key_links:
    - from: ".hero-carousel__track"
      to: ".hero-carousel__slide"
      via: "CSS overflow:hidden + flex layout"
      pattern: "hero-carousel__track"
    - from: ".hero-carousel__dot--active"
      to: "--accent"
      via: "background color token"
      pattern: "hero-carousel__dot--active"
---

<objective>
Remove the old `.hero__phones` CSS and replace it with a complete `/* ── Hero carousel ── */` CSS section. This creates every class the carousel HTML and JS will depend on in Plan 02.

Purpose: Establish the carousel's visual contract before the markup and script are written. Avoids style-less flash and keeps CSS changes isolated in their own commit.

Output: `index.html` with old fan-strip CSS deleted and new carousel CSS fully defined in the `<style>` block.
</objective>

<execution_context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/phases/02-hero-carousel/02-CONTEXT.md
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/PROJECT.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/ROADMAP.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Design tokens from index.html :root (lines 14–29) — use these, never hardcode values -->
:root {
  --bg:        #f5f5f7;
  --surface:   #ffffff;
  --text:      #1d1d1f;
  --muted:     #6e6e73;
  --accent:    #0071e3;
  --accent2:   #34c759;
  --radius-sm: 16px;
  --radius-md: 28px;
  --radius-lg: 40px;
  --shadow:    0 4px 32px rgba(0,0,0,0.08);
  --shadow-lg: 0 16px 64px rgba(0,0,0,0.14);
  --transition: 0.35s cubic-bezier(0.25,0.46,0.45,0.94);
}

<!-- Existing .phone-bezel CSS (lines 257–337) — KEEP this block entirely intact -->
<!-- Existing breakpoint block starts at line 877: @media (max-width: 768px) -->
<!-- CSS section comment format: /* ── Section Name ── */ -->
<!-- HTML section comment format: <!-- ══ Section Name ══ --> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old hero__phones CSS and add carousel CSS section</name>
  <files>index.html</files>
  <read_first>
    - index.html (entire `<style>` block, lines 10–902, to see current CSS state before any edits)
  </read_first>
  <action>
Make three targeted edits to the `<style>` block in `index.html`:

**Edit A — Delete the `.hero__phones` rule block (lines 246–254):**
Remove this entire block (8 lines including the comment):
```css
/* ── Hero phone mockup strip ── */
.hero__phones {
  display: flex;
  justify-content: center;
  gap: 2rem;
  margin-top: 6rem;
  animation: fadeUp 0.7s 0.4s ease both;
  flex-wrap: wrap;
}
```

**Edit B — Delete the tilted-phone transform rules (lines 334–337):**
Remove these 4 lines (including the inline comment):
```css
/* tilted hero phones */
.hero__phones .phone-bezel:nth-child(1) { transform: rotate(-5deg) translateY(20px); }
.hero__phones .phone-bezel:nth-child(2) { transform: rotate(0deg) translateY(0px); }
.hero__phones .phone-bezel:nth-child(3) { transform: rotate(5deg) translateY(20px); }
```
The `/* ── Phone bezel ── */` block (`.phone-bezel`, `.phone-bezel__frame`, etc.) immediately below MUST be preserved intact.

**Edit C — Replace the mobile `.hero__phones` override inside `@media (max-width: 768px)` (lines 884–887):**
Remove:
```css
.hero__phones { gap: 1rem; }
.hero__phones .phone-bezel { width: 170px; }
.hero__phones .phone-bezel:nth-child(1),
.hero__phones .phone-bezel:nth-child(3) { display: none; }
```
Replace with carousel mobile rules (insert at the same location):
```css
.hero-carousel { margin-top: 3rem; }
.hero-carousel__arrow { width: 36px; height: 36px; }
```

**Edit D — Insert new `/* ── Hero carousel ── */` CSS section.**
Insert this block immediately after the `/* ── Phone bezel ── */` section (after the `.phone-bezel__caption` rule, before `/* ── Elevator pitch ── */`). The full block to insert:

```css
/* ── Hero carousel ── */
.hero-carousel {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 6rem;
  animation: fadeUp 0.7s 0.4s ease both;
}

.hero-carousel__viewport {
  position: relative;
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

.hero-carousel__track-wrap {
  overflow: hidden;
  width: 240px;
}

.hero-carousel__track {
  display: flex;
  transition: transform var(--transition);
  will-change: transform;
}

.hero-carousel__slide {
  flex: 0 0 240px;
  width: 240px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* Arrow buttons */
.hero-carousel__arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  border: none;
  cursor: pointer;
  background: var(--surface);
  box-shadow: var(--shadow);
  color: var(--accent);
  transition: var(--transition);
  flex-shrink: 0;
}

.hero-carousel__arrow:hover {
  background: var(--accent);
  color: var(--surface);
  transform: scale(1.08);
}

.hero-carousel__arrow:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* Dot indicators */
.hero-carousel__dots {
  display: flex;
  gap: 0.5rem;
  margin-top: 1.25rem;
  list-style: none;
  padding: 0;
}

.hero-carousel__dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  border: none;
  cursor: pointer;
  background: rgba(0,0,0,0.2);
  transition: background var(--transition), transform var(--transition);
  padding: 0;
}

.hero-carousel__dot--active {
  background: var(--accent);
  transform: scale(1.25);
}
```
  </action>
  <verify>
    <automated>
      # Edit A — old hero__phones block gone
      grep -c 'hero__phones' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Edit B — tilted transform rules gone
      grep -c 'nth-child(1).*rotate' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # .phone-bezel intact
      grep -c 'phone-bezel__caption' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Edit D — carousel CSS section present
      grep -c 'hero-carousel' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Dot active class present
      grep -c 'hero-carousel__dot--active' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html

      # Token usage — no hardcoded colour in carousel rules
      grep 'hero-carousel__arrow' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html | grep -c 'var(--'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hero__phones' index.html` returns 0 (all old fan-strip CSS removed — the HTML markup replacement happens in Plan 02, but the CSS class must not exist in the style block)
    - Actually: after Plan 01 the HTML markup still references hero__phones until Plan 02 edits it — so `hero__phones` count may be >0 in the HTML body. Refine: `grep 'hero__phones' index.html` must NOT find any line inside the `<style>` block (lines 10–902). Check by confirming no `.hero__phones {` or `.hero__phones .` appears in the file after editing.
    - `grep -c '\.hero__phones {' index.html` returns 0
    - `grep -c 'rotate(-5deg)' index.html` returns 0
    - `grep -c 'rotate(5deg)' index.html` returns 0
    - `grep -c '\.hero-carousel {' index.html` returns 1 (the new carousel block opens)
    - `grep -c 'hero-carousel__track' index.html` is >= 1
    - `grep -c 'hero-carousel__dot--active' index.html` is >= 1
    - `grep -c 'hero-carousel__arrow' index.html` is >= 1
    - `grep 'hero-carousel__arrow' index.html` lines include `var(--surface)` and `var(--accent)` — no hardcoded hex colours inside the carousel arrow rules
    - `grep -c '\/\* ── Hero carousel ── \*\/' index.html` returns 1 (section banner present)
    - `.phone-bezel__caption {` still present (bezel CSS not accidentally deleted)
    - `grep -c 'hero-carousel__dot--active' index.html` returns 1 and the rule contains `background: var(--accent)`
  </acceptance_criteria>
  <done>
    The `<style>` block contains no `.hero__phones` rules, no tilted-phone transforms, no old mobile hero__phones override. It contains a complete `/* ── Hero carousel ── */` section with all BEM classes: `.hero-carousel`, `.hero-carousel__viewport`, `.hero-carousel__track-wrap`, `.hero-carousel__track`, `.hero-carousel__slide`, `.hero-carousel__arrow`, `.hero-carousel__dots`, `.hero-carousel__dot`, `.hero-carousel__dot--active`. All arrow styles use `--surface`, `--accent`, `--shadow` tokens. The `.phone-bezel` block is intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file → browser | index.html served as static asset; no server processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | index.html (static file) | accept | GitHub Pages serves the committed file; no runtime mutation path. Repository access controls are the appropriate mitigation (out of scope for this plan). |
| T-02-02 | Information Disclosure | CSS token values | accept | Design tokens are public UI values; no secrets in CSS custom properties. |
</threat_model>

<verification>
Open `index.html` in a browser. The page must load without CSS errors. The hero section should still render (phone bezels display upright — the carousel HTML is added in Plan 02). Confirm no `.hero__phones` CSS rules appear in browser DevTools for any element.
</verification>

<success_criteria>
- Old `.hero__phones` CSS block deleted from `<style>`
- Tilted-phone `nth-child` transform rules deleted
- Mobile `.hero__phones` override removed and replaced with carousel mobile rules
- New `/* ── Hero carousel ── */` CSS section present with all required BEM class definitions
- All carousel styles use CSS custom property tokens (no hardcoded colours)
- `.phone-bezel` and all its child rules remain untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-carousel/02-01-SUMMARY.md` using the summary template.
</output>
