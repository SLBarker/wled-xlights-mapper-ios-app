---
phase: 04-nav-download-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - UI-01

must_haves:
  truths:
    - "Nav links never wrap at any viewport width above 960px"
    - "Hamburger toggle appears only at 960px and below"
    - "Download CTA is visible in the nav bar at all viewport widths including mobile (alongside hamburger)"
    - "Download CTA shows a download SVG icon (circle + down-arrow) to the left of the text label"
    - "Download CTA hover produces translateY(-2px) lift and 0 12px 40px rgba(0,113,227,0.5) box-shadow"
  artifacts:
    - path: "index.html"
      provides: "Updated nav CSS and HTML — breakpoint, CTA position, CTA styles, icon, hover"
      contains: "960px"
  key_links:
    - from: ".site-nav__inner (HTML)"
      to: ".site-nav__cta (direct flex child)"
      via: "extracted from <ul class=\"site-nav__links\">"
      pattern: "site-nav__inner.*site-nav__cta|site-nav__cta.*site-nav__toggle"
    - from: ".site-nav__cta (CSS)"
      to: "display: inline-flex"
      via: "base rule update"
      pattern: "inline-flex"
    - from: ".site-nav__cta:hover (CSS)"
      to: "translateY\\(-2px\\)"
      via: "hover rule replacement"
      pattern: "translateY\\(-2px\\)"
---

<objective>
Apply all six targeted changes to `index.html` that fix nav wrapping, reposition the Download CTA, add the download icon, and update the CTA hover style to match the hero button.

Purpose: Nav links currently wrap at narrow desktop widths before the hamburger triggers; the Download CTA disappears on mobile because it hides with `.site-nav__links`; the hover style is inconsistent with the hero button.

Output: A single updated `index.html` where all five Phase 4 success criteria pass in a browser.
</objective>

<execution_context>
@/Users/simonbarker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonbarker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/phases/04-nav-download-cta/04-CONTEXT.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/CLAUDE.md
@/Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/.planning/ROADMAP.md

<interfaces>
<!-- Current nav CSS — lines 58–118, 957–990 (extracted for executor reference) -->

/* Lines 110–118 — current .site-nav__cta and its hover (BEFORE changes) */
.site-nav__cta {
  background: var(--accent);
  color: #fff !important;
  border-radius: 20px;
  padding: 0.4rem 1rem !important;
  transition: opacity var(--transition), transform var(--transition) !important;
}
.site-nav__cta:hover { opacity: 0.88; transform: scale(1.03); }

/* Lines 957–965 — .site-nav__drawer .site-nav__cta block (REMOVE entirely) */
.site-nav__drawer .site-nav__cta {
  display: block;
  margin: 8px 1.25rem 0;
  border-radius: 20px;
  background: var(--accent);
  color: #fff !important;
  text-align: center;
  padding: 0.6rem 1.25rem !important;
}

/* Lines 967–990 — responsive block threshold (768px → 960px) */
@media (max-width: 768px) { ... }

<!-- Current nav HTML — lines 999–1041 (extracted for executor reference) -->

<!-- Nav bar (lines 999–1027) — CTA currently inside <ul class="site-nav__links"> -->
<nav class="site-nav" aria-label="Site navigation">
  <div class="site-nav__inner">
    <a href="#hero" class="site-nav__logo" ...>WLED to XLights</a>
    <ul class="site-nav__links" id="site-nav-menu" role="list">
      <li><a href="#features">Features</a></li>
      ...
      <li><a href="#download" class="site-nav__cta">Download</a></li>  <!-- EXTRACT this -->
    </ul>
    <button class="site-nav__toggle" ...>...</button>
  </div>
</nav>

<!-- Drawer (lines 1029–1041) — CTA currently in drawer list (REMOVE) -->
<div class="site-nav__drawer" id="site-nav-drawer" aria-hidden="true">
  <ul role="list">
    ...
    <li><a href="#download" class="site-nav__cta">Download</a></li>  <!-- REMOVE this <li> -->
  </ul>
</div>

<!-- Hero Download button SVG icon (line 1057) — reference for icon paths/style -->
<svg width="16" height="16" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="2.5" aria-hidden="true">
  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"/>
  <path d="M8 12l4 4 4-4M12 8v8"/>
</svg>

<!-- Hero hover reference (lines 243–244) — exact values to replicate -->
.btn:hover { transform: translateY(-2px); opacity: 0.92; }
.btn-primary:hover { box-shadow: 0 12px 40px rgba(0,113,227,0.5); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update .site-nav__cta CSS — flex layout, box-shadow transition, hover style</name>

  <read_first>
    index.html lines 110–118 (current .site-nav__cta and :hover rules — must see exact current text before editing)
  </read_first>

  <files>index.html</files>

  <action>
Make three targeted edits to the CSS inside the `<style>` block. Do NOT alter any other rules.

**Edit A — Replace lines 110–118 (.site-nav__cta base rule and hover):**

Replace the current two rules:
```css
    .site-nav__cta {
      background: var(--accent);
      color: #fff !important;
      border-radius: 20px;
      padding: 0.4rem 1rem !important;
      transition: opacity var(--transition), transform var(--transition) !important;
    }

    .site-nav__cta:hover { opacity: 0.88; transform: scale(1.03); }
```

With these updated rules:
```css
    .site-nav__cta {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      background: var(--accent);
      color: #fff !important;
      border-radius: 20px;
      padding: 0.4rem 1rem !important;
      transition: transform var(--transition), box-shadow var(--transition) !important;
    }

    .site-nav__cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 40px rgba(0,113,227,0.5);
    }
```

Changes made in Edit A:
- Added `display: inline-flex; align-items: center; gap: 0.4rem;` (per D-06)
- Changed `transition` to include `box-shadow` and removed `opacity` (per D-08 and Claude's discretion note)
- Replaced `:hover` body: removed `opacity: 0.88; transform: scale(1.03)`, added `transform: translateY(-2px); box-shadow: 0 12px 40px rgba(0,113,227,0.5);` (per D-08)

**Edit B — Remove the .site-nav__drawer .site-nav__cta block (lines 957–965):**

Remove the entire 9-line block (no drawer CTA element will exist after HTML changes):
```css
    .site-nav__drawer .site-nav__cta {
      display: block;
      margin: 8px 1.25rem 0;
      border-radius: 20px;
      background: var(--accent);
      color: #fff !important;
      text-align: center;
      padding: 0.6rem 1.25rem !important;
    }
```

Delete this block entirely. The blank line before and after it may be removed to keep clean spacing.

**Edit C — Change responsive breakpoint threshold from 768px to 960px:**

Find (line ~967):
```css
    @media (max-width: 768px) {
```

Replace with:
```css
    @media (max-width: 960px) {
```

Only change the threshold value. All rules inside the `@media` block are unchanged.
  </action>

  <verify>
    <automated>grep -c "max-width: 960px" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c "768px" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c "inline-flex" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c "translateY(-2px)" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c "0 12px 40px rgba(0,113,227,0.5)" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c "scale(1.03)" /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "max-width: 960px" index.html` returns 1
    - `grep -c "768px" index.html` returns 0 (the old threshold is gone)
    - `grep -c "inline-flex" index.html` returns 1 (added to .site-nav__cta)
    - `grep -c "translateY(-2px)" index.html` returns at least 1 (appears in .site-nav__cta:hover; the hero .btn:hover also has it so count may be 2+)
    - `grep -c "0 12px 40px rgba(0,113,227,0.5)" index.html` returns 2 (one in .btn-primary:hover, one in .site-nav__cta:hover)
    - `grep -c "scale(1.03)" index.html` returns 0 (old hover rule removed)
    - `grep -c "site-nav__drawer .site-nav__cta" index.html` returns 0 (drawer CTA CSS block removed)
  </acceptance_criteria>

  <done>CSS is updated: breakpoint at 960px, .site-nav__cta is an inline-flex pill with box-shadow transition, :hover lifts with translateY(-2px) and blue glow, drawer CTA CSS block deleted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update nav HTML — extract CTA from links list, add SVG icon, remove from drawer</name>

  <read_first>
    index.html lines 999–1041 (current nav HTML — must see exact markup before editing)
  </read_first>

  <files>index.html</files>

  <action>
Make two targeted HTML edits inside the `<body>`. Do NOT alter any other markup.

**Edit A — Nav bar HTML (inside `<div class="site-nav__inner">`):**

Current structure (lines 1000–1026):
```html
    <div class="site-nav__inner">
      <a href="#hero" class="site-nav__logo" aria-label="WLED to XLights — Home">WLED to XLights</a>
      <ul class="site-nav__links" id="site-nav-menu" role="list">
        <li><a href="#features">Features</a></li>
        <li><a href="#how-it-works">How It Works</a></li>
        <li><a href="#export">Using Your Export</a></li>
        <li><a href="#requirements">Requirements</a></li>
        <li><a href="#tips">Tips</a></li>
        <li><a href="#contact">Contact</a></li>
        <li><a href="#privacy">Privacy</a></li>
        <li><a href="#download" class="site-nav__cta">Download</a></li>
      </ul>
      <button
        class="site-nav__toggle"
        ...>
      </button>
    </div>
```

Replace with this updated structure — remove the CTA `<li>` from the list, add the bare `<a>` with SVG icon between the `</ul>` and the `<button>`:

```html
    <div class="site-nav__inner">
      <a href="#hero" class="site-nav__logo" aria-label="WLED to XLights — Home">WLED to XLights</a>
      <ul class="site-nav__links" id="site-nav-menu" role="list">
        <li><a href="#features">Features</a></li>
        <li><a href="#how-it-works">How It Works</a></li>
        <li><a href="#export">Using Your Export</a></li>
        <li><a href="#requirements">Requirements</a></li>
        <li><a href="#tips">Tips</a></li>
        <li><a href="#contact">Contact</a></li>
        <li><a href="#privacy">Privacy</a></li>
      </ul>
      <a href="#download" class="site-nav__cta">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"/><path d="M8 12l4 4 4-4M12 8v8"/></svg>
        Download
      </a>
      <button
        class="site-nav__toggle"
        aria-label="Open navigation menu"
        aria-expanded="false"
        aria-controls="site-nav-drawer"
        type="button">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none"
             stroke="var(--text)" stroke-width="2" stroke-linecap="round"
             aria-hidden="true">
          <line x1="3" y1="6"  x2="21" y2="6"  class="nav-bar nav-bar--top"/>
          <line x1="3" y1="12" x2="21" y2="12" class="nav-bar nav-bar--mid"/>
          <line x1="3" y1="18" x2="21" y2="18" class="nav-bar nav-bar--bot"/>
        </svg>
      </button>
    </div>
```

Key changes:
- Removed `<li><a href="#download" class="site-nav__cta">Download</a></li>` from the `<ul>` (per D-02)
- Added bare `<a href="#download" class="site-nav__cta">` with the 14×14 SVG icon as a direct flex child of `.site-nav__inner`, between `</ul>` and `<button>` (per D-02, D-03, D-07)
- The `<a>` is NOT wrapped in `<li>` — it is a direct flex child
- SVG: `width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"` with the two paths from the hero button (per D-07)

**Edit B — Drawer HTML (inside `<div class="site-nav__drawer">`):**

Current structure (lines 1029–1041):
```html
  <div class="site-nav__drawer" id="site-nav-drawer" aria-hidden="true">
    <ul role="list">
      <li><a href="#features">Features</a></li>
      <li><a href="#how-it-works">How It Works</a></li>
      <li><a href="#export">Using Your Export</a></li>
      <li><a href="#requirements">Requirements</a></li>
      <li><a href="#tips">Tips</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="#privacy">Privacy</a></li>
      <li><a href="#download" class="site-nav__cta">Download</a></li>
    </ul>
  </div>
```

Replace with (remove the Download CTA `<li>` entirely, per D-04):
```html
  <div class="site-nav__drawer" id="site-nav-drawer" aria-hidden="true">
    <ul role="list">
      <li><a href="#features">Features</a></li>
      <li><a href="#how-it-works">How It Works</a></li>
      <li><a href="#export">Using Your Export</a></li>
      <li><a href="#requirements">Requirements</a></li>
      <li><a href="#tips">Tips</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="#privacy">Privacy</a></li>
    </ul>
  </div>
```
  </action>

  <verify>
    <automated>grep -c 'site-nav__cta' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c 'site-nav__drawer.*site-nav__cta\|site-nav__cta.*site-nav__drawer' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c 'width="14" height="14"' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
    <automated>grep -c 'M12 2C6.48' /Users/simonbarker/Documents/projects/landingPage/wled-xlights-mapper-ios-app/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'site-nav__cta' index.html` returns 1 (only the nav bar `<a>` — no longer in the links list or drawer)
    - The single `.site-nav__cta` occurrence is a bare `<a>` element NOT inside a `<li>` — verify with: `grep -B2 'site-nav__cta' index.html` showing the preceding line is `</ul>` not `<li>`
    - `grep -c 'width="14" height="14"' index.html` returns 1 (the nav CTA SVG)
    - `grep -c 'M12 2C6.48' index.html` returns 2 (one in nav CTA, one in hero button)
    - `grep -c 'M8 12l4 4 4-4M12 8v8' index.html` returns 2 (one in nav CTA, one in hero button)
    - The drawer `<ul>` contains exactly 7 `<li>` items (Features, How It Works, Using Your Export, Requirements, Tips, Contact, Privacy) — no Download entry
    - `grep -c 'aria-hidden="true"' index.html` increases by 1 compared to before (the nav CTA SVG adds one more)
  </acceptance_criteria>

  <done>Nav bar CTA is a direct flex child of .site-nav__inner with SVG icon at 14×14 and text label "Download". The CTA `<li>` is gone from the links list and from the drawer. The drawer has 7 items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → Browser | No dynamic data; only href="#download" anchor navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | `href="#download"` on CTA | accept | Internal anchor only; no external URL, no user input, no auth context |
| T-04-02 | Information Disclosure | SVG `aria-hidden="true"` | accept | Decorative icon with visible text label present; screen readers get the label |
</threat_model>

<verification>
After both tasks complete, open `index.html` directly in a browser (no server needed) and verify:

1. **Nav wrapping (NAV-01):** Resize the browser window from 1400px down to 961px. Nav links remain on a single line and do not wrap at any width above 960px.
2. **Hamburger trigger (NAV-01):** At 960px and below, `.site-nav__links` hides and the hamburger button appears.
3. **CTA always visible (NAV-02):** At any viewport width (including 400px mobile), the Download CTA pill is visible in the nav bar alongside the hamburger button.
4. **CTA not in drawer (NAV-02):** Open the hamburger drawer at mobile width — the drawer does NOT contain a Download link.
5. **CTA icon (NAV-03):** The Download CTA shows a small circle-with-arrow icon to the left of the "Download" text.
6. **CTA hover (UI-01):** Hover the nav Download CTA — it lifts upward and shows a blue glow shadow. No scale animation.

Automated checks (run from project root):
```bash
grep -c "max-width: 960px" index.html   # must be 1
grep -c "768px" index.html              # must be 0
grep -c "inline-flex" index.html        # must be 1
grep -c "translateY(-2px)" index.html   # must be >= 1
grep -c "scale(1.03)" index.html        # must be 0
grep -c "site-nav__cta" index.html      # must be 1
grep -c 'width="14" height="14"' index.html   # must be 1
```
</verification>

<success_criteria>
- Nav links do not wrap at any viewport width above 960px (breakpoint updated from 768px)
- Hamburger toggle appears only at 960px and below
- Download CTA pill is present in the nav bar at all viewport widths (direct flex child of .site-nav__inner, not inside .site-nav__links)
- Download CTA shows the download SVG icon (14×14, circle + down-arrow) alongside the "Download" text label
- Download CTA hover: `transform: translateY(-2px)` + `box-shadow: 0 12px 40px rgba(0,113,227,0.5)` — matching hero button style exactly
- Download CTA does NOT appear in the mobile drawer
</success_criteria>

<output>
After completion, create `.planning/phases/04-nav-download-cta/04-01-SUMMARY.md` using the summary template.
</output>
