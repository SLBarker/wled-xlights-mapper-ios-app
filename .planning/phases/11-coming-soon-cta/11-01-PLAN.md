---
phase: 11-coming-soon-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: false
requirements: [CTA-01, CTA-02, CTA-03]

must_haves:
  truths:
    - "Nav Download pill displays the text 'Coming Soon' at all viewport widths"
    - "Hero Download button displays the text 'Coming Soon'"
    - "Clicking or tapping either CTA produces no navigation, scroll, or anchor jump"
    - "Both CTAs render at reduced opacity with cursor: not-allowed and no hover lift/glow"
    - "Both CTAs remain keyboard-focusable and announce a disabled state to screen readers"
  artifacts:
    - path: "index.html"
      provides: "Disabled 'Coming Soon' CTAs (nav pill + hero button) with disabled CSS"
      contains: "aria-disabled=\"true\""
  key_links:
    - from: "index.html nav pill <a class=\"site-nav__cta\">"
      to: "CSS rule [aria-disabled=\"true\"]"
      via: "aria-disabled attribute selector"
      pattern: "aria-disabled=.true."
    - from: "index.html hero button <a class=\"btn btn-primary\">"
      to: "CSS rule [aria-disabled=\"true\"]"
      via: "aria-disabled attribute selector"
      pattern: "aria-disabled=.true."
---

<objective>
Replace the two broken `#download` placeholder CTAs (desktop nav pill and hero button) with a clearly disabled "Coming Soon" state. Both CTAs must communicate unavailability through text and appearance alone, must not navigate or scroll when activated, and must remain keyboard-accessible.

Purpose: The `#download` anchor is broken (it self-references the hero button's own `id`, producing a no-op jump). Until the real App Store URL is available (CONT-01, deferred), visitors must immediately understand the app is "Coming Soon" rather than encountering broken download links.

Output: Modified `index.html` — two CTA elements changed (text, icon removal, attributes) plus a disabled-state CSS rule in the inline `<style>` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coming-soon-cta/11-CONTEXT.md

<interfaces>
<!-- Current state of the two CTA elements and their CSS. Executor uses these directly. -->

Nav pill — index.html line ~1340:
```html
<a href="#download" class="site-nav__cta">
  <svg width="14" height="14" ... aria-hidden="true">...</svg>
  Download
</a>
```

Hero button — index.html line ~1372:
```html
<a href="#download" class="btn btn-primary" id="download">
  <svg width="16" height="16" ... aria-hidden="true">...</svg>
  Download on the App Store
</a>
```

Existing CSS — `.site-nav__cta` lines 153-168, `.btn`/`.btn-primary` lines 267-294.
The `.btn` transition already includes `opacity`. `.site-nav__cta` transition covers `transform` and `box-shadow`.
Hover rules to suppress: `.site-nav__cta:hover` (transform + box-shadow), `.btn:hover` (transform + opacity), `.btn-primary:hover` (box-shadow).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert both CTA elements to disabled "Coming Soon" state</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines ~1335-1380 — the nav pill and hero button markup, to see current attributes and inner content)
    - .planning/phases/11-coming-soon-cta/11-CONTEXT.md (decisions D-01, D-07, D-08 govern this task)
  </read_first>
  <action>
    Modify the two download CTA `<a>` elements in index.html.

    Nav pill (line ~1340, `class="site-nav__cta"`):
    - Remove the `href="#download"` attribute entirely (per D-07 — no href means no navigation on a static page).
    - Delete the child `<svg>` download-arrow icon element entirely (per D-01 — text-only, no replacement icon).
    - Change the visible text from `Download` to `Coming Soon` (per CTA-01).
    - Add `aria-disabled="true"` to the `<a>` (per D-08 — preserves keyboard focus, announces disabled state).
    - Do NOT add or remove a `tabindex` attribute — the element keeps default link focusability.

    Hero button (line ~1372, `class="btn btn-primary" id="download"`):
    - Remove the `href="#download"` attribute entirely (per D-07).
    - Remove the `id="download"` attribute — it is the self-referential anchor target; with no other links pointing to `#download` (confirmed in CONTEXT integration points) it is now dead.
    - Delete the child `<svg>` download-arrow icon element entirely (per D-01).
    - Change the visible text from `Download on the App Store` to `Coming Soon` (per CTA-01).
    - Add `aria-disabled="true"` to the `<a>` (per D-08).
    - Do NOT add or remove a `tabindex` attribute.

    Keep both elements as `<a>` tags with their existing `class` values unchanged — the existing button-styled-anchor pattern continues. Do not add a `title` attribute (per D-05).
  </action>
  <verify>
    <automated>grep -c 'aria-disabled="true"' index.html | grep -qx 2 && ! grep -q 'href="#download"' index.html && ! grep -q 'id="download"' index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains exactly 2 occurrences of `aria-disabled="true"`
    - `index.html` contains 0 occurrences of `href="#download"`
    - `index.html` contains 0 occurrences of `id="download"`
    - The nav pill `<a class="site-nav__cta">` has visible text `Coming Soon` and no child `<svg>`
    - The hero `<a class="btn btn-primary">` has visible text `Coming Soon` and no child `<svg>`
    - Both `<a>` elements retain their original `class` attribute values
    - Neither `<a>` element has a `tabindex` attribute
  </acceptance_criteria>
  <done>Both CTAs read "Coming Soon", carry `aria-disabled="true"`, have no `href`/`id="download"`/icon, and remain `<a>` elements with unchanged classes.</done>
</task>

<task type="auto">
  <name>Task 2: Add disabled-state CSS for aria-disabled CTAs</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 153-168 — `.site-nav__cta` and `.site-nav__cta:hover` rules)
    - index.html (lines 267-294 — `.btn`, `.btn-primary`, `.btn:hover`, `.btn-primary:hover` rules)
    - .planning/phases/11-coming-soon-cta/11-CONTEXT.md (decisions D-02, D-03, D-04, D-06, D-09 govern this task)
  </read_first>
  <action>
    Add CSS to the inline `<style>` block in index.html to give both `aria-disabled="true"` CTAs a disabled appearance. Use a single shared attribute-selector rule keyed on `[aria-disabled="true"]` (preferred per CONTEXT discretion note — single source of truth with the HTML attribute).

    Add a base rule `[aria-disabled="true"]` that applies:
    - `opacity: 0.6;` (per D-02 — 60%, Apple-style disabled convention)
    - `cursor: not-allowed;` (per D-03, satisfies CTA-03)
    - `pointer-events: none;` (per D-09 — belt-and-suspenders; blocks mouse/touch hover, does not affect keyboard focus)

    Add hover-suppression rules so the buttons do not lift or glow on mouseover (per D-04). Because `pointer-events: none` already blocks mouse-driven `:hover` on the elements themselves, also add explicit override rules to be safe and self-documenting:
    - `.site-nav__cta[aria-disabled="true"]:hover` → `transform: none;` and `box-shadow: none;`
    - `.btn[aria-disabled="true"]:hover` → `transform: none;` and `opacity: 0.6;` (keep opacity at the disabled value, not the active `0.92`)
    - `.btn-primary[aria-disabled="true"]:hover` → `box-shadow: none;`

    Do NOT change the background color of either button — keep the existing `var(--accent)` blue (per D-06); opacity alone communicates the disabled state. Place the new rules near the existing CTA rules in the `<style>` block (after line ~294 or grouped with the `.btn`/`.site-nav__cta` rules — Claude's call). Do not create a separate `.cta--disabled` class.
  </action>
  <verify>
    <automated>grep -q 'aria-disabled="true"\]' index.html && grep -A4 'aria-disabled="true"\]' index.html | grep -q 'opacity: 0.6' && grep -A4 'aria-disabled="true"\]' index.html | grep -q 'cursor: not-allowed' && grep -A4 'aria-disabled="true"\]' index.html | grep -q 'pointer-events: none' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` `<style>` block contains a rule with selector `[aria-disabled="true"]`
    - That rule declares `opacity: 0.6`, `cursor: not-allowed`, and `pointer-events: none`
    - `index.html` contains a `:hover` rule scoped to `[aria-disabled="true"]` that sets `transform: none`
    - The hover-suppression rules set `box-shadow: none` for the disabled CTAs
    - No new `background` declaration for the disabled state — `var(--accent)` remains the CTA background
    - No `.cta--disabled` class is introduced
    - All CSS remains inline in the `<style>` block — no new `.css` file created
  </acceptance_criteria>
  <done>A shared `[aria-disabled="true"]` rule applies opacity 0.6, not-allowed cursor, and pointer-events none; hover rules suppress transform/box-shadow; button background unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify disabled CTA behavior and accessibility</name>
  <what-built>
    Both download CTAs (desktop nav pill and hero button) were converted to a disabled "Coming Soon" state: text changed to "Coming Soon", download-arrow icons removed, `href`/`id="download"` removed, `aria-disabled="true"` added, and a disabled CSS rule (opacity 0.6, cursor not-allowed, pointer-events none, hover suppressed) added inline.
  </what-built>
  <how-to-verify>
    1. Open `index.html` directly in a browser (no server needed — static file).
    2. Desktop width: confirm the nav pill (top-right) reads "Coming Soon", appears dimmed (~60% opacity), has no arrow icon.
    3. Confirm the hero button reads "Coming Soon", appears dimmed, has no arrow icon.
    4. Hover the mouse over each CTA — confirm NO upward lift and NO glow/shadow enhancement; the cursor shows the not-allowed symbol.
    5. Click each CTA — confirm the page does NOT scroll or jump and the URL does NOT change (no `#download` appended).
    6. Resize the browser to a narrow mobile width — confirm the nav pill still reads "Coming Soon" (the mobile drawer has no Download link, so only the desktop pill is in scope).
    7. Press Tab repeatedly — confirm both CTAs still receive a visible focus outline (keyboard focus preserved).
    8. If a screen reader is available, confirm each CTA is announced as "disabled" or "dimmed".
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (wrong text, missing dimming, button still navigates, focus lost).</resume-signal>
</task>

</tasks>

<verification>
- `grep -c 'aria-disabled="true"' index.html` returns `2`
- `grep -c 'href="#download"' index.html` returns `0`
- `grep -c 'id="download"' index.html` returns `0`
- Both CTAs display the literal text `Coming Soon`
- The inline `<style>` block contains an `[aria-disabled="true"]` rule with `opacity: 0.6`, `cursor: not-allowed`, `pointer-events: none`
- No new `.css` or `.js` file created — `index.html` remains the single deployable artifact
</verification>

<success_criteria>
- Nav Download pill shows "Coming Soon" at all viewport widths (CTA-01)
- Hero Download button shows "Coming Soon" (CTA-01)
- Clicking/tapping either button produces no navigation, scroll, or anchor jump (CTA-02)
- Both buttons are visually distinct: opacity 0.6, cursor: not-allowed, hover animation suppressed (CTA-03)
- Both buttons remain keyboard-focusable with no tabindex removal — `aria-disabled` announces disabled state (roadmap success criterion 5)
</success_criteria>

<output>
Create `.planning/phases/11-coming-soon-cta/11-01-SUMMARY.md` when done
</output>
