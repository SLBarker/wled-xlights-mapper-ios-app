---
phase: 10-font-self-hosting
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - index.html
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "Page load makes zero requests to fonts.googleapis.com or fonts.gstatic.com"
    - "Inter renders from local resources/fonts/ files via @font-face"
    - "All 4 weights (400, 500, 600, 700) are declared with font-display: swap"
  artifacts:
    - path: "index.html"
      provides: "@font-face declarations for self-hosted Inter; Google Fonts links removed"
      contains: "@font-face"
  key_links:
    - from: "index.html @font-face src"
      to: "resources/fonts/inter-NNN.woff2"
      via: "url('resources/fonts/inter-NNN.woff2') format('woff2')"
      pattern: "url\\(['\"]?resources/fonts/inter-(400|500|600|700)\\.woff2"
---

<objective>
Remove the Google Fonts CDN `<link>` tags from `index.html` `<head>` and replace them with 4 self-hosted `@font-face` declarations at the top of the inline `<style>` block, pointing at the WOFF2 files committed in Plan 10-01.

Purpose: This is the change that actually satisfies PERF-02 — after it, the page loads Inter entirely from GitHub Pages assets with no outbound Google Fonts request.
Output: Modified `index.html` with no Google Fonts links and 4 local `@font-face` rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-font-self-hosting/10-CONTEXT.md
@.planning/phases/10-font-self-hosting/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove Google Fonts links from index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (read the full <head> block, lines 1-25, to confirm exact current Google Fonts tags before editing)
    - .planning/phases/10-font-self-hosting/10-CONTEXT.md (canonical_refs: which tags to remove)
  </read_first>
  <action>
    In `index.html` `<head>`, delete these tags entirely:
    - `<link rel="preconnect" href="https://fonts.googleapis.com">` (currently line 23)
    - `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">` (currently line 24)
    - `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` — IF this tag is present anywhere in `<head>` (CONTEXT lists it as a tag to remove; the current file at lines 23-24 may not include it). Search the head for `fonts.gstatic.com` and remove any matching `<link>` if found.

    Do NOT add a `<link rel="preload">` for the fonts — D-05 explicitly excludes preload hints. Leave every other `<head>` tag (meta, og:, twitter:, canonical, sitemap, icon links) exactly as-is. The `<style>` opening tag on the next line stays.
  </action>
  <verify>
    <automated>grep -c 'fonts.googleapis.com\|fonts.gstatic.com' index.html | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'fonts.googleapis.com' index.html` returns `0`
    - Source assertion: `grep -c 'fonts.gstatic.com' index.html` returns `0`
    - Source assertion: `grep -c 'rel="preload"' index.html` returns `0` (no preload hint added — D-05)
    - Behavior: all Open Graph, Twitter, canonical, sitemap, and icon `<link>`/`<meta>` tags remain present (`grep -c 'og:title' index.html` returns `1`, `grep -c 'rel="canonical"' index.html` returns `1`)
  </acceptance_criteria>
  <done>index.html `<head>` contains no reference to fonts.googleapis.com or fonts.gstatic.com and no rel="preload" tag; all other head tags untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 4 @font-face declarations at top of inline style block</name>
  <files>index.html</files>
  <read_first>
    - index.html (read the <style> block opening and the "Reset & tokens" comment region, lines 25-30, to identify the exact insertion point)
    - .planning/phases/10-font-self-hosting/10-CONTEXT.md (D-01 weights, D-03 WOFF2 only, D-04 font-display: swap, discretion: place at top of style block)
    - .planning/phases/10-font-self-hosting/10-01-SUMMARY.md (confirm the committed font file names)
  </read_first>
  <action>
    Insert 4 `@font-face` declarations immediately after the `<style>` opening tag and BEFORE the `/* Reset & tokens */` comment and the `*, *::before, *::after` reset rule. Add a `/* Self-hosted Inter */` comment above the block.

    Each of the 4 declarations uses these exact properties:
    - `font-family: 'Inter';`
    - `src: url('resources/fonts/inter-NNN.woff2') format('woff2');` — where NNN is the weight; path is relative with NO leading slash (index.html and resources/ are both at repo root)
    - `font-weight: NNN;` — 400, 500, 600, 700 respectively
    - `font-style: normal;`
    - `font-display: swap;`

    Produce one declaration per weight: 400 -> inter-400.woff2, 500 -> inter-500.woff2, 600 -> inter-600.woff2, 700 -> inter-700.woff2. Do NOT declare a weight-300 face (D-01 drops it). Do NOT add a WOFF fallback in `src` (D-03 — WOFF2 only).

    Do NOT modify the existing `body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; }` rule — the fallback stack is already correct and unchanged.
  </action>
  <verify>
    <automated>grep -c "@font-face" index.html | grep -qx 4 && grep -c "font-display: swap" index.html | grep -qx 4 && grep -c "resources/fonts/inter-" index.html | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '@font-face' index.html` returns `4`
    - Source assertion: `grep -c "format('woff2')" index.html` returns `4`
    - Source assertion: `grep -c 'font-display: swap' index.html` returns `4`
    - Source assertion: `grep -oc 'resources/fonts/inter-400.woff2\|resources/fonts/inter-500.woff2\|resources/fonts/inter-600.woff2\|resources/fonts/inter-700.woff2' index.html` — all 4 distinct paths appear (each `grep -c` for an individual path returns `1`)
    - Source assertion: `grep -c 'inter-300' index.html` returns `0` (no weight-300 face — D-01)
    - Behavior: the 4 `@font-face` blocks appear in the source BEFORE the line containing `*, *::before, *::after` (verify with `grep -n '@font-face' index.html` line numbers all less than the `grep -n '\\*, \\*::before' index.html` line number)
    - Source assertion: `grep -c "font-family: 'Inter', -apple-system" index.html` returns `1` (existing body font stack unchanged)
  </acceptance_criteria>
  <done>index.html inline `<style>` block opens with 4 `@font-face` declarations (weights 400/500/600/700, WOFF2-only src to resources/fonts/, font-display: swap), placed before the reset/token rules; body font-family stack unchanged.</done>
</task>

</tasks>

<verification>
- No fonts.googleapis.com or fonts.gstatic.com strings remain in index.html.
- Exactly 4 @font-face declarations, all with font-display: swap and WOFF2-only src.
- @font-face block precedes the CSS reset.
- Manual browser check (deferred to UAT): hard-reload with Network panel open shows zero requests to Google Fonts domains; Inter renders identically on desktop and mobile.
</verification>

<success_criteria>
- index.html loads Inter exclusively from resources/fonts/ local WOFF2 files.
- Zero Google Fonts CDN references in the file.
- All 4 weights declared with swap behavior, satisfying PERF-02.
</success_criteria>

<output>
Create `.planning/phases/10-font-self-hosting/10-02-SUMMARY.md` when done.
</output>
