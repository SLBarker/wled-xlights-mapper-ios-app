---
phase: 09-crawl-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robots.txt
  - sitemap.xml
  - index.html
autonomous: true
requirements: [DISC-03]

must_haves:
  truths:
    - "Fetching /robots.txt returns valid content containing Allow: / for all crawlers"
    - "Fetching /sitemap.xml returns valid XML listing the canonical landing page URL"
    - "robots.txt points crawlers to the sitemap via a Sitemap: directive"
    - "index.html head declares the sitemap via a link rel=sitemap tag"
  artifacts:
    - path: "robots.txt"
      provides: "Crawler permissions and sitemap pointer"
      contains: "Allow: /"
    - path: "sitemap.xml"
      provides: "Canonical URL listing for search engines"
      contains: "<loc>https://slbarker.github.io/wled-xlights-mapper-ios-app/</loc>"
    - path: "index.html"
      provides: "Sitemap discovery link in head"
      contains: "rel=\"sitemap\""
  key_links:
    - from: "robots.txt"
      to: "sitemap.xml"
      via: "Sitemap: directive"
      pattern: "Sitemap: https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml"
    - from: "index.html"
      to: "sitemap.xml"
      via: "link rel=sitemap href"
      pattern: "rel=\"sitemap\".*href=\"/sitemap.xml\""
---

<objective>
Replace the crawler-blocking `robots.txt` with one that allows all search engine crawling and points to a new `sitemap.xml`, create that minimal `sitemap.xml` listing the canonical landing page URL, and add a `<link rel="sitemap">` discovery tag to `index.html`.

Purpose: Search engines currently see `Disallow: /` and skip the site entirely. This phase opens the page to indexing — a prerequisite for the page being discoverable in search results (satisfies DISC-03).
Output: Updated `robots.txt`, new `sitemap.xml`, one-line addition to `index.html` `<head>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-crawl-infrastructure/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open robots.txt to all crawlers and add Sitemap directive</name>
  <files>robots.txt</files>
  <read_first>
    - robots.txt — current content is exactly two lines: `User-agent: *` then `Disallow: /`; this task replaces the file content
    - .planning/phases/09-crawl-infrastructure/09-CONTEXT.md — D-03 and D-04 specify the exact final content
  </read_first>
  <action>
    Replace the entire contents of `robots.txt` (per D-03, D-04). The file currently contains `User-agent: *` then `Disallow: /`. The new content must be exactly four lines, in this order: line 1 `User-agent: *`, line 2 `Allow: /`, line 3 blank, line 4 `Sitemap: https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml`. Remove the `Disallow: /` line entirely — do not leave it commented or alongside the Allow line. The file ends with a single trailing newline. Use the exact sitemap URL `https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml`.
  </action>
  <verify>
    <automated>grep -q '^Allow: /$' robots.txt &amp;&amp; grep -q '^Sitemap: https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml$' robots.txt &amp;&amp; ! grep -q 'Disallow' robots.txt &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `robots.txt` contains a line exactly `Allow: /`
    - `robots.txt` contains a line exactly `Sitemap: https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml`
    - `robots.txt` contains no occurrence of the string `Disallow`
    - `robots.txt` contains a line exactly `User-agent: *`
    - `grep -q '^Allow: /$' robots.txt && grep -q '^Sitemap: ' robots.txt && ! grep -q 'Disallow' robots.txt` exits 0
  </acceptance_criteria>
  <done>robots.txt allows all crawlers (`Allow: /`), contains no `Disallow` directive, and declares the sitemap via the `Sitemap:` directive.</done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap.xml listing the canonical landing page URL</name>
  <files>sitemap.xml</files>
  <read_first>
    - .planning/phases/09-crawl-infrastructure/09-CONTEXT.md — D-01 (single `<loc>` entry, privacy.html excluded), D-02 (minimal format, no optional elements), and Claude's Discretion note (XML declaration + namespace)
    - .planning/codebase/STRUCTURE.md — confirms repo root is the static site root so `sitemap.xml` at repo root is served at `/sitemap.xml`
  </read_first>
  <action>
    Create a new file `sitemap.xml` at the repo root (per D-01, D-02). Line 1 is the XML declaration `<?xml version="1.0" encoding="UTF-8"?>`. The root element is `<urlset>` with attribute `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"`. Inside `<urlset>` place exactly one `<url>` element containing exactly one `<loc>` element with the value `https://slbarker.github.io/wled-xlights-mapper-ios-app/` (trailing slash included, matching the canonical URL used in index.html). Do NOT include `<lastmod>`, `<changefreq>`, or `<priority>` elements — minimal format only. Do NOT include `privacy.html` — only the landing page is listed. End the file with a single trailing newline.
  </action>
  <verify>
    <automated>test -f sitemap.xml &amp;&amp; grep -q '<loc>https://slbarker.github.io/wled-xlights-mapper-ios-app/</loc>' sitemap.xml &amp;&amp; grep -q 'xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"' sitemap.xml &amp;&amp; ! grep -q 'lastmod\|changefreq\|priority\|privacy' sitemap.xml &amp;&amp; python3 -c "import xml.etree.ElementTree as ET; ET.parse('sitemap.xml')" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `sitemap.xml` exists at the repo root
    - First line is exactly `<?xml version="1.0" encoding="UTF-8"?>`
    - Contains `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">`
    - Contains exactly one `<loc>` element with value `https://slbarker.github.io/wled-xlights-mapper-ios-app/`
    - Contains no `<lastmod>`, `<changefreq>`, `<priority>`, or `privacy` strings
    - `python3 -c "import xml.etree.ElementTree as ET; ET.parse('sitemap.xml')"` exits 0 (well-formed XML)
  </acceptance_criteria>
  <done>`sitemap.xml` exists at repo root, is well-formed XML, and lists exactly the canonical landing page URL with a single `<loc>` entry and no optional metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add sitemap discovery link to index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html — read lines 1–24 of `<head>`; the `<link rel="canonical" href="https://slbarker.github.io/wled-xlights-mapper-ios-app/">` is at line 18, followed by favicon `<link>` tags at lines 19–21
    - .planning/phases/09-crawl-infrastructure/09-CONTEXT.md — D-05 specifies the exact tag and placement
  </read_first>
  <action>
    Insert one new line into `index.html` `<head>` immediately after the existing canonical link tag (`<link rel="canonical" href="https://slbarker.github.io/wled-xlights-mapper-ios-app/">`, currently line 18) and before the `<link rel="icon" type="image/svg+xml" ...>` favicon tag (per D-05). The new line is exactly `  <link rel="sitemap" type="application/xml" href="/sitemap.xml">` (two-space indentation matching the surrounding `<head>` tags). Do not modify, reorder, or remove any other tag in `<head>`. This is the only change to `index.html` in this phase.
  </action>
  <verify>
    <automated>grep -q '<link rel="sitemap" type="application/xml" href="/sitemap.xml">' index.html &amp;&amp; grep -A1 'rel="canonical"' index.html | grep -q 'rel="sitemap"' &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains the exact line `<link rel="sitemap" type="application/xml" href="/sitemap.xml">`
    - The `rel="sitemap"` link appears on the line immediately after the `rel="canonical"` link (`grep -A1 'rel="canonical"' index.html | grep -q 'rel="sitemap"'` exits 0)
    - No other `<head>` tag was added, removed, or reordered (canonical, OG, twitter, favicon, preconnect, font tags all unchanged)
  </acceptance_criteria>
  <done>`index.html` `<head>` contains a `<link rel="sitemap" type="application/xml" href="/sitemap.xml">` tag positioned directly after the canonical link, with no other head changes.</done>
</task>

</tasks>

<verification>
- `robots.txt`: `grep -q '^Allow: /$' robots.txt && ! grep -q 'Disallow' robots.txt` — crawlers allowed
- `robots.txt`: contains `Sitemap: https://slbarker.github.io/wled-xlights-mapper-ios-app/sitemap.xml`
- `sitemap.xml`: well-formed XML (`python3 -c "import xml.etree.ElementTree as ET; ET.parse('sitemap.xml')"`) with one `<loc>` of the canonical URL
- `index.html`: contains `<link rel="sitemap" type="application/xml" href="/sitemap.xml">` after the canonical link
- Both `robots.txt` and `sitemap.xml` live at the repo root so GitHub Pages serves them at `/robots.txt` and `/sitemap.xml`
</verification>

<success_criteria>
1. `robots.txt` is served at the root URL and contains `Allow: /` for all crawlers (and no `Disallow`).
2. `sitemap.xml` is served at the root URL and lists the canonical page URL with a `<loc>` entry.
3. Fetching `robots.txt` and `sitemap.xml` directly returns valid content (no 404) — both files exist at repo root, sitemap is well-formed XML.
4. DISC-03 satisfied: robots.txt allows all crawling, sitemap.xml lists the canonical page URL.
</success_criteria>

<output>
Create `.planning/phases/09-crawl-infrastructure/09-01-SUMMARY.md` when done.
</output>
