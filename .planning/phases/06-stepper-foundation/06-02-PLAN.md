---
phase: 06-stepper-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - index.html
autonomous: true
requirements:
  - STEP-01
  - STEP-05
  - STEP-06
  - STEP-07
  - STEP-08
must_haves:
  truths:
    - "Old <div class='workflow'> block is removed from the HTML"
    - "Two .stepper blocks exist inside #how-it-works, each preceded by a <p class='workflow__phase-title'> eyebrow"
    - "Each .stepper block has exactly 5 .stepper__item elements"
    - "Each .stepper__item contains a <button class='stepper__header'> with .stepper__badge and .stepper__title, plus a .stepper__panel with a .stepper__body"
    - "Step 1 in each stepper has class stepper__header--active and aria-expanded=true; steps 2-5 have aria-expanded=false"
    - "'In the App' steps 1-4 have a .stepper__media with a .phone-bezel and the correct screenshot <img>; step 5 uses .stepper__body--text-only"
    - "All 5 'Importing into xLights' steps use .stepper__body--text-only"
    - "animatedEls querySelectorAll no longer references .step-card; it references .stepper__item"
    - "All existing step text content is preserved verbatim"
  artifacts:
    - path: "index.html"
      provides: "Two .stepper HTML blocks in #how-it-works"
      contains: "class=\"stepper\""
    - path: "index.html"
      provides: "Phone bezel screenshots for In the App steps 1-4"
      contains: "resources/screenshot/connect.jpeg"
    - path: "index.html"
      provides: "Updated JS selector"
      contains: ".stepper__item"
  key_links:
    - from: "button.stepper__header"
      to: "div.stepper__panel"
      via: "aria-controls pointing to panel id"
      pattern: "aria-controls=\"panel-app-1\""
    - from: ".stepper__media"
      to: ".phone-bezel"
      via: "child element reusing existing bezel classes"
      pattern: "stepper__media.*phone-bezel"
    - from: "animatedEls querySelectorAll"
      to: ".stepper__item"
      via: "selector string replacement"
      pattern: "\\.stepper__item"
---

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<objective>
Replace the `<div class="workflow">` HTML block with two new `.stepper` elements matching the BEM structure defined in Plan 06-01's CSS, then update the JS `animatedEls` selector so the new stepper items receive scroll-reveal animation.

Purpose: This is the visible deliverable of Phase 6. After this plan executes, both workflow sections render as fully-formed stepper components with all step titles visible, phone bezel screenshots on the correct steps, and correct responsive layout.

Output: `index.html` with new stepper HTML in place, old workflow HTML removed, and JS selector updated.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-stepper-foundation/06-CONTEXT.md
@.planning/phases/06-stepper-foundation/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace workflow HTML with two stepper blocks</name>

  <read_first>
  Read index.html lines 1373–1455 to see the exact current state of the #how-it-works section and confirm the line range of the `<div class="workflow">` block to remove.
  Read .planning/phases/06-stepper-foundation/06-CONTEXT.md (decisions D-01 through D-06) for HTML structure rules.
  Read .planning/phases/06-stepper-foundation/06-UI-SPEC.md "BEM Structure" and "Phone Bezel Reuse" sections for class names and screenshot assignments.
  </read_first>

  <files>index.html</files>

  <action>
  Remove the `<div class="workflow" aria-label="In-app workflow steps">…</div>` block (lines 1379–1448) including all its children (both `.workflow__phase` divs and all step content within).

  Replace it with the following exact HTML. Insert it in the same position — directly inside `<div class="container">`, after the `<h2>` heading.

  IMPORTANT structural rules (per D-01 through D-06):
  - Each `.stepper` block has `.stepper__item` elements only — NO separate `.stepper__rail` wrapper
  - `.stepper__header` is the button; it uses class `stepper__header--active` (NOT `stepper__tab--active`)
  - Step 1 in each stepper: `class="stepper__header stepper__header--active"` and `aria-expanded="true"`
  - Steps 2–5: `class="stepper__header"` and `aria-expanded="false"`
  - Panel `id` attributes: `panel-app-1` through `panel-app-5` for "In the App"; `panel-xlights-1` through `panel-xlights-5` for "Importing into xLights"
  - `aria-controls` on each button matches its panel's `id`
  - `.stepper__content` contains an `<h3>` (same title text as `.stepper__title`) followed by `<p>` description elements
  - `.stepper__body` modifier: use `class="stepper__body"` when a screenshot is present; use `class="stepper__body stepper__body--text-only"` when no screenshot

  Replace with this HTML block:

  ```html
  <!-- ── In the App stepper ── -->
  <p class="workflow__phase-title">In the App</p>
  <div class="stepper" aria-label="In-app workflow steps">

    <!-- Step 1: Connect (screenshot: connect.jpeg) -->
    <div class="stepper__item">
      <button
        class="stepper__header stepper__header--active"
        aria-expanded="true"
        aria-controls="panel-app-1"
      >
        <span class="stepper__badge" aria-hidden="true">1</span>
        <span class="stepper__title">Connect</span>
      </button>
      <div class="stepper__panel" id="panel-app-1">
        <div class="stepper__body">
          <div class="stepper__media">
            <div class="phone-bezel">
              <div class="phone-bezel__notch"></div>
              <div class="phone-bezel__frame">
                <div class="phone-bezel__screen">
                  <img src="resources/screenshot/connect.jpeg" alt="Connect screen showing WLED controller connection UI" loading="lazy">
                </div>
              </div>
            </div>
          </div>
          <div class="stepper__content">
            <h3>Connect</h3>
            <p>Open the app and tap to connect to a WLED controller.</p>
            <p>Use mDNS discovery to connect automatically or manually enter WLED IP address.</p>
            <p>Save multiple named configurations for different installations.</p>
            <p>Select which segments to include for multi-segment controllers.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 2: Scan (screenshot: ar.jpeg) -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-app-2"
      >
        <span class="stepper__badge" aria-hidden="true">2</span>
        <span class="stepper__title">Scan</span>
      </button>
      <div class="stepper__panel" id="panel-app-2">
        <div class="stepper__body">
          <div class="stepper__media">
            <div class="phone-bezel">
              <div class="phone-bezel__notch"></div>
              <div class="phone-bezel__frame">
                <div class="phone-bezel__screen">
                  <img src="resources/screenshot/ar.jpeg" alt="AR scan screen showing LED overlay in real time" loading="lazy">
                </div>
              </div>
            </div>
          </div>
          <div class="stepper__content">
            <h3>Scan</h3>
            <p>Tap "Scan new Object"</p>
            <p>Move to a position where the LEDs are visible, then tap "Start Scan".</p>
            <p>The scan cycles a binary pattern on the LEDs that are captured and analyzed.</p>
            <p>Change viewpoint and repeat.</p>
            <p>LEDs resolved from 2 or more scan positions are shown on a live AR overlay in real time.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 3: Report (screenshot: results.jpeg) -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-app-3"
      >
        <span class="stepper__badge" aria-hidden="true">3</span>
        <span class="stepper__title">Report</span>
      </button>
      <div class="stepper__panel" id="panel-app-3">
        <div class="stepper__body">
          <div class="stepper__media">
            <div class="phone-bezel">
              <div class="phone-bezel__notch"></div>
              <div class="phone-bezel__frame">
                <div class="phone-bezel__screen">
                  <img src="resources/screenshot/results.jpeg" alt="Results screen showing scan metrics summary" loading="lazy">
                </div>
              </div>
            </div>
          </div>
          <div class="stepper__content">
            <h3>Report</h3>
            <p>When scanning is complete the app summarises results, providing metrics on the scanned object.</p>
            <p>Go back to rescan, proceed to preview, or export directly.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 4: Preview (screenshot: preview.jpeg) -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-app-4"
      >
        <span class="stepper__badge" aria-hidden="true">4</span>
        <span class="stepper__title">Preview</span>
      </button>
      <div class="stepper__panel" id="panel-app-4">
        <div class="stepper__body">
          <div class="stepper__media">
            <div class="phone-bezel">
              <div class="phone-bezel__notch"></div>
              <div class="phone-bezel__frame">
                <div class="phone-bezel__screen">
                  <img src="resources/screenshot/preview.jpeg" alt="3D preview screen with transform gizmo controls" loading="lazy">
                </div>
              </div>
            </div>
          </div>
          <div class="stepper__content">
            <h3>Preview</h3>
            <p>Open the 3D preview to inspect results.</p>
            <p>Use the transform gizmo to adjust its position.</p>
            <p>Delete the calculated position and replace with an interpolated position based on its neighbours.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 5: Export (no screenshot — text-only) -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-app-5"
      >
        <span class="stepper__badge" aria-hidden="true">5</span>
        <span class="stepper__title">Export</span>
      </button>
      <div class="stepper__panel" id="panel-app-5">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Export</h3>
            <p>Tap the export button</p>
            <p>Use the iOS share sheet to save the <code>.xmodel</code> file to the Files app or AirDrop it directly to your Mac.</p>
          </div>
        </div>
      </div>
    </div>

  </div><!-- /.stepper (In the App) -->

  <!-- ── Importing into xLights stepper ── -->
  <p class="workflow__phase-title" style="margin-top: 64px;">Importing into xLights</p>
  <div class="stepper" aria-label="Steps to import the .xmodel file into xLights">

    <!-- Step 1: Open xLights -->
    <div class="stepper__item">
      <button
        class="stepper__header stepper__header--active"
        aria-expanded="true"
        aria-controls="panel-xlights-1"
      >
        <span class="stepper__badge" aria-hidden="true">1</span>
        <span class="stepper__title">Open xLights</span>
      </button>
      <div class="stepper__panel" id="panel-xlights-1">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Open xLights</h3>
            <p>Open xLights and go to the <strong>Layout</strong> tab.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 2: Import Model -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-xlights-2"
      >
        <span class="stepper__badge" aria-hidden="true">2</span>
        <span class="stepper__title">Import Model</span>
      </button>
      <div class="stepper__panel" id="panel-xlights-2">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Import Model</h3>
            <p>Click the <strong>Import Model</strong> button in the toolbar.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 3: Place Model -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-xlights-3"
      >
        <span class="stepper__badge" aria-hidden="true">3</span>
        <span class="stepper__title">Place Model</span>
      </button>
      <div class="stepper__panel" id="panel-xlights-3">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Place Model</h3>
            <p>Drag on the canvas to place the model's position in your layout, then release — a file picker opens.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 4: Select File -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-xlights-4"
      >
        <span class="stepper__badge" aria-hidden="true">4</span>
        <span class="stepper__title">Select File</span>
      </button>
      <div class="stepper__panel" id="panel-xlights-4">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Select File</h3>
            <p>Select the exported <strong>.xmodel</strong> file.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 5: Done -->
    <div class="stepper__item">
      <button
        class="stepper__header"
        aria-expanded="false"
        aria-controls="panel-xlights-5"
      >
        <span class="stepper__badge" aria-hidden="true">5</span>
        <span class="stepper__title">Done</span>
      </button>
      <div class="stepper__panel" id="panel-xlights-5">
        <div class="stepper__body stepper__body--text-only">
          <div class="stepper__content">
            <h3>Done</h3>
            <p>The model appears in your xLights layout as a Custom Model. Rename it and wire it to the correct controller and channel as usual.</p>
          </div>
        </div>
      </div>
    </div>

  </div><!-- /.stepper (Importing into xLights) -->
  ```
  </action>

  <verify>
  ```
  grep -c 'class="stepper__item"' index.html
  ```
  Returns 10 (5 per stepper × 2 steppers).

  ```
  grep -c 'stepper__header--active' index.html
  ```
  Returns at least 2 (one in each stepper — the CSS rule + the two HTML instances = at least 3 total; HTML alone must be 2).

  ```
  grep -c 'aria-expanded="true"' index.html
  ```
  Returns 2 (one per stepper, on step 1 only).

  ```
  grep -c 'stepper__body--text-only' index.html
  ```
  Returns at least 6 (step 5 of "In the App" + all 5 "Importing into xLights" steps = 6 in HTML; plus 1 CSS rule).

  ```
  grep -n 'connect.jpeg\|ar.jpeg\|results.jpeg\|preview.jpeg' index.html
  ```
  Returns 4 matches — one per "In the App" step 1–4.

  ```
  grep -c 'class="workflow"' index.html
  ```
  Returns 0 (old workflow div is gone).

  ```
  grep -n 'panel-app-1\|panel-xlights-1' index.html
  ```
  Returns matches for both `aria-controls` and `id` attributes.
  </verify>

  <done>
  The old `<div class="workflow">` block is removed. Two `.stepper` elements are present in `#how-it-works`. Each has 5 `.stepper__item` children with correct button/panel structure, aria attributes, and step content. "In the App" steps 1–4 have phone bezel + screenshot; step 5 uses `.stepper__body--text-only`. All 5 "Importing into xLights" steps use `.stepper__body--text-only`. All step text content matches the source material.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update animatedEls JS selector</name>

  <read_first>
  Read index.html lines 1610–1640 to see the exact current state of the animatedEls querySelectorAll and the IntersectionObserver callback before making changes.
  </read_first>

  <files>index.html</files>

  <action>
  Find the `animatedEls` querySelectorAll call (line ~1615). It currently reads:

  ```javascript
  const animatedEls = document.querySelectorAll(
    '.feature-row, .feature-card, .step-card, .req-card, .tip-item, .privacy-card, .contact-cta'
  );
  ```

  Replace `.step-card` with `.stepper__item` in the selector string. The result must be:

  ```javascript
  const animatedEls = document.querySelectorAll(
    '.feature-row, .feature-card, .stepper__item, .req-card, .tip-item, .privacy-card, .contact-cta'
  );
  ```

  Change only the selector token — do not alter any other part of the querySelectorAll call, the IntersectionObserver callback, or any surrounding code.
  </action>

  <verify>
  ```
  grep -n '\.step-card' index.html
  ```
  Returns 0 matches outside of CSS comments (all `.step-card` references are now inside `/* … */` comment blocks — none are active selectors or JS strings).

  ```
  grep -n '\.stepper__item' index.html
  ```
  Returns at least 2 matches: one in the CSS section and one in the JS querySelectorAll string.

  ```
  grep -n 'querySelectorAll' index.html
  ```
  Returns a match whose string contains `.stepper__item` and does NOT contain `.step-card`.
  </verify>

  <done>
  The `animatedEls` querySelectorAll string contains `.stepper__item` instead of `.step-card`. No other code is altered. The IntersectionObserver will now apply scroll-reveal animation to the new stepper items, matching the CSS `opacity: 0; transform: translateY(20px)` base state declared in Plan 06-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static single-file HTML served via GitHub Pages; no user input, no auth, no backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | index.html (GitHub Pages) | accept | Read-only static hosting; no server-side writes; Git history provides audit trail |
| T-06-02-02 | Information Disclosure | img alt attributes | accept | Alt text describes UI only; no PII or sensitive data in attributes |
| T-06-02-03 | Spoofing | aria-controls / id linkage | accept | aria-controls points to same-document ids; no cross-origin trust boundary |
</threat_model>

<verification>
After both tasks complete, validate the full deliverable:

1. Open index.html in a browser at ≥768px viewport width:
   - Both stepper sections render as a horizontal 5-column rail of step title buttons
   - Step 1's panel is visible below each rail; other panels are hidden
   - "In the App" step 1 panel shows connect.jpeg in a phone bezel beside the text
   - "In the App" step 5 panel shows text at full width (no bezel column)

2. Resize to ≤767px:
   - Both steppers render as vertical lists
   - Step 1's panel is visible (CSS `:first-child` rule); other panels are collapsed (height 0)
   - Phone bezel in "In the App" step 1 renders at 180px, centred, stacked above text

3. Run grep checks:
   - `grep -c 'class="stepper__item"' index.html` returns 10
   - `grep -c 'class="workflow"' index.html` returns 0
   - `grep -n 'stepper__item' index.html` — matches in both CSS and JS
   - `grep -n 'step-card' index.html` — only matches inside `/* … */` comments
</verification>

<success_criteria>
- Old `<div class="workflow">` block is completely removed
- Two `.stepper` blocks (one per workflow phase) are in `#how-it-works`, each with 5 `.stepper__item` children
- Each `.stepper__item` has a `<button class="stepper__header">` with `.stepper__badge` and `.stepper__title` child spans
- Step 1 in each stepper has `class="stepper__header stepper__header--active"` and `aria-expanded="true"`
- Steps 2–5 have `aria-expanded="false"` and no `--active` modifier
- "In the App" steps 1–4 include `.stepper__media > .phone-bezel` with the correct screenshot `<img>`
- "In the App" step 5 and all "Importing into xLights" steps use `class="stepper__body stepper__body--text-only"`
- All `aria-controls` values match their corresponding panel `id` attributes
- `animatedEls` querySelectorAll contains `.stepper__item`, not `.step-card`
- Page renders without JS or CSS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-stepper-foundation/06-stepper-foundation-02-SUMMARY.md` using the summary template.
</output>
