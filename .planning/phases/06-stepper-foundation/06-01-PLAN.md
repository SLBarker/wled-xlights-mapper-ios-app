---
phase: 06-stepper-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - STEP-01
  - STEP-05
  - STEP-06
  - STEP-07
  - STEP-08
must_haves:
  truths:
    - "Old workflow CSS rules are commented out and no longer apply to any rendered HTML"
    - "New .stepper CSS rules are present and complete — all BEM elements have explicit style declarations"
    - "Desktop layout (>=768px) uses CSS Grid with display:contents to produce a horizontal rail of headers and a shared panel row"
    - "Mobile layout (<=767px) uses display:block for accordion stacking"
    - "Inactive panels are hidden via display:none; the active panel is shown via adjacent-sibling selector"
    - "Focus ring rule (.stepper__header:focus-visible) is present using var(--accent)"
    - "Scroll-reveal base styles (opacity:0, translateY(20px)) are declared on .stepper__item matching the existing pattern"
  artifacts:
    - path: "index.html"
      provides: "Complete stepper CSS section"
      contains: ".stepper__header--active"
    - path: "index.html"
      provides: "Commented-out legacy CSS"
      contains: "/* .workflow {"
  key_links:
    - from: ".stepper__item"
      to: "CSS Grid on .stepper"
      via: "display:contents"
      pattern: "stepper__item.*display.*contents"
    - from: ".stepper__header--active"
      to: ".stepper__panel"
      via: "adjacent sibling selector"
      pattern: "stepper__header--active.*\\+.*stepper__panel"
    - from: ".stepper__body--text-only"
      to: ".stepper__content"
      via: "flex:1 modifier"
      pattern: "stepper__body--text-only.*stepper__content"
---

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<objective>
Comment out all old workflow CSS and write a complete new `/* ── Stepper ── */` CSS section with every BEM rule the stepper component needs, including the new 767px media block for mobile accordion layout.

Purpose: Establish the CSS foundation so that the HTML restructure in Plan 06-02 can be dropped into a page that already knows how to render it. No visible change until Plan 06-02 replaces the HTML.

Output: `index.html` with old workflow CSS commented out and new stepper CSS section present after it.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-stepper-foundation/06-CONTEXT.md
@.planning/phases/06-stepper-foundation/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment out old workflow CSS</name>

  <read_first>
  Read index.html lines 560–700 to see the exact current state of the old workflow CSS block before touching anything.
  Read index.html lines 995–1013 to see the 960px media block containing the .workflow__steps override at line 1006.
  </read_first>

  <files>index.html</files>

  <action>
  Comment out the following CSS rule groups by wrapping each one in `/* … */`. Do NOT delete them — they stay as a safety net. Leave `.workflow__phase-title` (lines 573–580) intact and uncommented; it is reused by the new stepper structure.

  Groups to comment out (use the exact line ranges as a guide — verify against what you read):

  1. `.workflow { margin-top: 60px; }` (lines 565–568)
  2. `.workflow__phase { margin-bottom: 60px; }` (lines 569–572)
  3. `.workflow__steps { display: grid; … }` (lines 582–586)
  4. `.step-card { … }` through `.step-card p { … }` including `.step-card.visible` and `.step-card__num` (lines 588–619)
  5. `/* Quality tier pills */` block: `.quality-tiers { … }` through `.tier-red::before { … }` (lines 621–654)
  6. `/* xlights import steps */` block: `.numbered-list { … }` through `.numbered-list strong { … }` (lines 657–695)

  Then find line 1006 inside the `@media (max-width: 960px)` block and comment out ONLY that single rule:
  `.workflow__steps { grid-template-columns: 1fr; }`
  Leave the surrounding rules (`.section`, `.feature-cards`, `.req-grid`, `.privacy-grid`) completely untouched.

  Result: Six commented-out blocks in the how-it-works CSS area; one commented-out line in the 960px media block; `.workflow__phase-title` still active.
  </action>

  <verify>
  ```
  grep -c '/\* \.workflow {' index.html
  ```
  Returns 1 (the commented-out `.workflow` rule).

  ```
  grep -c '/\* \.workflow__steps' index.html
  ```
  Returns at least 1 (covers the main block; 960px block may be inline-commented differently).

  ```
  grep -n '\.workflow__phase-title' index.html
  ```
  Returns at least one uncommented line (the rule block is still active — no `/*` before it).

  ```
  grep -n '\.step-card' index.html
  ```
  Every match is inside a `/* … */` comment — no bare `.step-card {` selector is active.
  </verify>

  <done>
  All old workflow CSS is wrapped in block comments. `.workflow__phase-title` rule block remains uncommented and active. The `.workflow__steps` override inside the 960px media block is commented out. The file still parses as valid CSS (no unclosed comment blocks).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add complete stepper CSS section</name>

  <read_first>
  Re-read index.html lines 560–700 after Task 1 completes to confirm the old CSS is commented out and to identify the exact insertion point.
  Read index.html lines 960–1014 to see the 960px media block and confirm the position of the existing `@media (max-width: 960px)` rule — the new 767px block goes after it (before the closing `</style>` tag).
  Read .planning/phases/06-stepper-foundation/06-UI-SPEC.md (already in context) for exact CSS values.
  </read_first>

  <files>index.html</files>

  <action>
  Insert a new CSS section immediately after the last commented-out workflow CSS block (after the `.numbered-list` comment block, before `.icon-list`). The section banner must follow the project's established format.

  Insert this exact CSS block:

  ```css
  /* ── Stepper ── */

  /* Base: scroll-reveal starting state (mirrors .step-card pattern) */
  .stepper__item {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  .stepper__item.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Desktop layout (>=768px): CSS Grid — items become layout-transparent */
  .stepper {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    margin-top: 16px;
  }

  .stepper__item {
    display: contents;
  }

  /* Rail: headers land in grid row 1 */
  .stepper__header {
    grid-row: 1;
    width: 100%;
    padding: 12px 16px;
    display: flex;
    align-items: center;
    gap: 8px;
    background: transparent;
    border: none;
    border-bottom: 2px solid transparent;
    cursor: pointer;
    text-align: left;
    color: var(--muted);
    transition: border-color var(--transition), color var(--transition), background var(--transition);
  }

  .stepper__header:hover {
    background: var(--surface);
    color: var(--text);
  }

  .stepper__header:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* Active tab styles */
  .stepper__header--active {
    background: var(--surface);
    border-bottom-color: var(--accent);
    color: var(--text);
  }

  .stepper__header--active .stepper__badge {
    background: var(--accent);
  }

  /* Step number badge */
  .stepper__badge {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--muted);
    color: #fff;
    font-size: 0.75rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  /* Step title text in the rail button */
  .stepper__title {
    font-size: 0.9375rem;
    font-weight: 600;
    line-height: 1.3;
    text-align: left;
  }

  /* Panel: lands in grid row 2, spans all 5 columns, hidden by default */
  .stepper__panel {
    grid-row: 2;
    grid-column: 1 / -1;
    display: none;
    background: var(--surface);
    border-radius: 0 0 var(--radius-sm) var(--radius-sm);
    box-shadow: var(--shadow);
    padding: 24px;
    margin-top: 16px;
  }

  /* Show the panel that immediately follows the active header
     (works through display:contents — header and panel remain DOM siblings) */
  .stepper__header--active + .stepper__panel {
    display: block;
  }

  /* Panel body: flex row — media column beside text column */
  .stepper__body {
    display: flex;
    gap: 24px;
    align-items: flex-start;
  }

  /* No-screenshot modifier: content fills full width */
  .stepper__body--text-only .stepper__content {
    flex: 1;
  }

  /* Media column: phone bezel slot */
  .stepper__media {
    flex-shrink: 0;
    width: 240px;
  }

  /* Content column: step heading + description */
  .stepper__content {
    flex: 1;
    min-width: 0;
  }

  .stepper__content h3 {
    font-size: 1.0625rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: var(--text);
  }

  .stepper__content p {
    font-size: 0.9375rem;
    font-weight: 400;
    line-height: 1.6;
    color: var(--muted);
    margin-bottom: 0.75em;
  }

  .stepper__content p:last-child {
    margin-bottom: 0;
  }
  ```

  Then, after the existing `@media (max-width: 960px)` block (and before the closing `</style>` tag), insert this new media block:

  ```css
  /* ── Stepper: mobile accordion (<=767px) ── */
  @media (max-width: 767px) {

    /* Override desktop grid — restore normal block flow */
    .stepper {
      display: block;
      margin-top: 0;
    }

    .stepper__item {
      display: block;
      border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    }

    /* Accordion header button */
    .stepper__header {
      grid-row: unset;
      width: 100%;
      padding: 16px 0;
      display: flex;
      align-items: center;
      gap: 8px;
      background: none;
      border: none;
      border-bottom: none;
      cursor: pointer;
      text-align: left;
      color: var(--muted);
    }

    .stepper__header--active {
      border-bottom: none;
      background: none;
    }

    .stepper__header--active .stepper__title {
      color: var(--text);
    }

    /* Panel: collapsed by default; Phase 7 JS will toggle open class */
    .stepper__panel {
      grid-row: unset;
      grid-column: unset;
      display: block;
      height: 0;
      overflow: hidden;
      padding: 0;
      margin-top: 0;
      box-shadow: none;
      border-radius: 0;
      transition: height var(--transition);
    }

    /* First item open by default via CSS (Phase 7 will manage this via JS) */
    .stepper__item:first-child .stepper__panel {
      height: auto;
      padding: 0 0 16px;
    }

    .stepper__item:first-child .stepper__header .stepper__badge {
      background: var(--accent);
    }

    /* Panel body stacks vertically on mobile */
    .stepper__body {
      flex-direction: column;
      gap: 16px;
    }

    /* Phone bezel: smaller, centred, stacked above text */
    .stepper__media {
      width: 180px;
      margin: 0 auto 16px;
    }
  }
  ```

  Verify by inspecting the file after writing. Both the desktop section and the 767px media block must be present.
  </action>

  <verify>
  ```
  grep -n '/* ── Stepper ── */' index.html
  ```
  Returns one match in the `<style>` block.

  ```
  grep -n '@media (max-width: 767px)' index.html
  ```
  Returns one match (the new stepper mobile block).

  ```
  grep -n 'stepper__header--active.*+.*stepper__panel\|stepper__header--active + .stepper__panel' index.html
  ```
  Returns at least one match (the adjacent-sibling display rule).

  ```
  grep -n 'display: contents' index.html
  ```
  Returns at least one match (`.stepper__item` desktop rule).

  ```
  grep -n 'stepper__body--text-only' index.html
  ```
  Returns at least one match in the CSS section.

  ```
  grep -n 'grid-row: 2' index.html
  ```
  Returns at least one match (`.stepper__panel` placed in row 2).
  </verify>

  <done>
  New `/* ── Stepper ── */` CSS section is present in index.html after the commented-out workflow CSS, before `.icon-list`. The 767px media block is present after the 960px block before `</style>`. All BEM elements have style declarations. The `.stepper__header--active + .stepper__panel` adjacent-sibling rule controls panel visibility. `.stepper__item` uses `display:contents` on desktop and `display:block` on mobile.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static single-file HTML served via GitHub Pages; no user input, no auth, no backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | index.html (GitHub Pages) | accept | Read-only static hosting; no server-side writes; Git history provides audit trail |
| T-06-02 | Information Disclosure | CSS selectors | accept | No PII in CSS; class names are non-sensitive identifiers |
| T-06-03 | Denial of Service | Static asset delivery | accept | GitHub Pages CDN handles availability; no application-level DoS surface |
</threat_model>

<verification>
After both tasks complete, validate the CSS section by checking:

1. `grep -n 'stepper__header--active + .stepper__panel' index.html` — returns one CSS rule with `display: block`
2. `grep -n '\.workflow {' index.html` — all matches are inside block comments (`/* … */`)
3. `grep -n '\.workflow__phase-title' index.html` — at least one uncommented rule block present
4. `grep -n '@media (max-width: 767px)' index.html` — exactly one match
5. The file loads in a browser without CSS parse errors (open DevTools console — no errors)
</verification>

<success_criteria>
- Old `.workflow`, `.workflow__phase`, `.workflow__steps`, `.step-card`, `.quality-tiers`, `.tier-pill`, `.numbered-list` CSS rules are all commented out
- `.workflow__phase-title` CSS rule is untouched and active
- `.workflow__steps` override inside `@media (max-width: 960px)` is commented out
- New `/* ── Stepper ── */` section is present with all BEM rules
- `@media (max-width: 767px)` block is present with mobile accordion rules
- No other existing CSS rules are altered
- `index.html` is valid and serves without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-stepper-foundation/06-stepper-foundation-01-SUMMARY.md` using the summary template.
</output>
